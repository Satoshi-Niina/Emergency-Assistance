# 本番環境統合手順書

## 📋 **事前準備**

### 1. **バックアップ作成**
```bash
# 本番環境のバックアップ
git checkout main
git pull origin main
git checkout -b backup-before-integration-$(date +%Y%m%d)
git push origin backup-before-integration-$(date +%Y%m%d)

# ローカル修正内容の確認
git checkout main
git diff HEAD~10 --name-only
```

### 2. **変更ファイル一覧の確認**
```bash
# 修正されたファイルの確認
git status
git diff --name-only
```

## 🔍 **修正内容の確認**

### フロントエンド変更
- `client/src/pages/chat.tsx` - AI支援メッセージ処理の修正
- `client/src/lib/structured-diagnostic-manager.ts` - 新規追加（使用していない場合は削除）

### バックエンド変更
- `server/local-server.js` - AI支援プロンプトと後処理関数の追加
- 新規APIエンドポイント: `/api/knowledge-base/search`

## 🚀 **段階的統合手順**

### ステップ1: フロントエンドの統合
```bash
# 1. フロントエンドの変更をコミット
git add client/src/pages/chat.tsx
git commit -m "feat: AI支援の段階的応急処置フロー実装"

# 2. 不要なファイルの削除（使用していない場合）
rm client/src/lib/structured-diagnostic-manager.ts
git add -A
git commit -m "cleanup: 不要なファイルの削除"
```

### ステップ2: バックエンドの統合
```bash
# 1. バックエンドの変更をコミット
git add server/local-server.js
git commit -m "feat: AI支援プロンプト改善と後処理関数追加"

# 2. 新規APIエンドポイントの追加
git add server/local-server.js
git commit -m "feat: ナレッジベース検索API追加"
```

### ステップ3: 環境変数の確認
```bash
# 本番環境で必要な環境変数の確認
echo "OPENAI_API_KEY: ${OPENAI_API_KEY:0:10}..."
echo "NODE_ENV: $NODE_ENV"
```

## 🔧 **本番環境でのテスト**

### 1. **APIエンドポイントのテスト**
```bash
# ローカルでAPIテスト
curl -X POST http://localhost:3000/api/chatgpt \
  -H "Content-Type: application/json" \
  -d '{"text":"テスト","aiSupportMode":true,"simpleMode":true}'

# ナレッジベース検索APIテスト
curl -X POST http://localhost:3000/api/knowledge-base/search \
  -H "Content-Type: application/json" \
  -d '{"query":"エンジン","limit":5}'
```

### 2. **フロントエンドのテスト**
```bash
# フロントエンドのビルドテスト
cd client
npm run build
npm run preview
```

## 🚨 **本番環境での注意点**

### 1. **環境変数の設定**
```bash
# 本番環境で必要な環境変数
export OPENAI_API_KEY="your-production-api-key"
export NODE_ENV="production"
```

### 2. **依存関係の確認**
```bash
# package.jsonの依存関係確認
npm list --depth=0
```

### 3. **ログの監視**
```bash
# 本番環境でのログ監視
tail -f /var/log/your-app/error.log
```

## 🔄 **ロールバック手順**

### 緊急時のロールバック
```bash
# 1. 直前のコミットに戻す
git reset --hard HEAD~1
git push origin main --force

# 2. バックアップブランチから復元
git checkout backup-before-integration-$(date +%Y%m%d)
git checkout main
git reset --hard backup-before-integration-$(date +%Y%m%d)
git push origin main --force
```

## 📊 **統合後の確認項目**

### 1. **機能テスト**
- [ ] AI支援ボタンの動作
- [ ] 段階的質問の表示
- [ ] 1つの質問のみの表示
- [ ] 時間表示の動作
- [ ] エラーハンドリング

### 2. **パフォーマンステスト**
- [ ] ページ読み込み速度
- [ ] API応答時間
- [ ] メモリ使用量

### 3. **セキュリティテスト**
- [ ] APIエンドポイントの認証
- [ ] 入力値の検証
- [ ] エラーメッセージの適切性

## 🎯 **推奨統合順序**

1. **ステージング環境でのテスト**
2. **フロントエンドの統合**
3. **バックエンドの統合**
4. **新規APIエンドポイントの追加**
5. **本番環境でのテスト**
6. **監視とログ確認**

この手順に従って、安全に本番環境に統合してください。
