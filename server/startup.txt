# Azure App Service Startup Configuration
# Windows Web App - Node.js Application

# Primary startup command (CommonJS wrapper -> ES Module)
node app.js

# Fallback startup commands (if primary fails):
# npm start
# node server.js
# node azure-server.js

# Debug startup command:
# node --inspect app.js
