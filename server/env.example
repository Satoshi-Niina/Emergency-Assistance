# GENERATED by AI agent on 2025-09-26
# Server Environment Variables Example

# Node Environment
NODE_ENV=production

# Server Configuration
PORT=8000

# JWT Secret (32+ characters)
JWT_SECRET=your-32-character-secret-key-here-change-this-in-production

# Session Secret (32+ characters)
SESSION_SECRET=your-32-character-session-secret-here-change-this-in-production

# Database Connection
# Format: postgresql://username:password@host:port/database?sslmode=require
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# Frontend URL (for CORS)
FRONTEND_URL=https://your-swa-url.azurestaticapps.net

# Azure Storage Configuration
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=<account>;AccountKey=<key>;EndpointSuffix=core.windows.net
AZURE_STORAGE_CONTAINER_NAME=knowledge

# Storage Base Prefix (default: knowledge-base)
STORAGE_BASE_PREFIX=knowledge-base

# Optional: Public base URL for storage
STORAGE_PUBLIC_BASE_URL=https://<account>.blob.core.windows.net/<container>

# Trust Proxy (for Azure App Service)
TRUST_PROXY=1
