{
  "name": "emergency-assistance",
  "version": "1.0.2",
  "description": "Emergency Assistance System - Full Stack Application",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "node server/unified-hot-reload-server.js",
    "start": "npm run dev",
    "build": "npm --prefix client run build",
    "prebuild": "echo 🔨 Building client for production deployment...",
    "postbuild": "echo ✅ Client build completed. Ready for deployment!",
    "deploy-check": "node scripts/check-build-before-push.js",
    "pre-push": "npm run deploy-check",
    "postinstall": "npm --prefix server ci --no-audit --no-fund || npm --prefix server install && npm --prefix client ci --no-audit --no-fund || npm --prefix client install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  },
  "keywords": [
    "emergency",
    "assistance",
    "azure",
    "nodejs"
  ],
  "author": "Emergency Assistance Team",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/Satoshi-Niina/Emergency-Assistance.git"
  }
}
