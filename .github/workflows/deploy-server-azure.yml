name: Deploy Server to Azure App Service (Simplified)

on:
  push:
    branches:
      - main
      - master
      - backup
    paths:
      - 'server/**'
      - 'shared/**'
      - 'knowledge-base/**'
      - '.github/workflows/deploy-server-azure.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy to Azure App Service
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies (config-safe)
        working-directory: ./server
        run: |
          echo "📦 Installing server dependencies..."
          
          # Clean start - remove problematic files
          rm -rf node_modules package-lock.json .npm .npmrc
          
          # Create minimal .npmrc to avoid config issues
          echo "audit=false" > .npmrc
          echo "fund=false" >> .npmrc
          echo "progress=false" >> .npmrc
          echo "legacy-peer-deps=true" >> .npmrc
          
          # Simple npm install without config commands
          echo "🔧 Running npm install..."
          npm install || {
            echo "⚠️ npm install failed, trying with --force..."
            npm install --force
          }
          
          # Basic verification
          echo "📦 Installation summary:"
          echo "  - Modules installed: $(ls node_modules 2>/dev/null | wc -l || echo '0')"
          echo "  - Express: $(test -d node_modules/express && echo 'OK' || echo 'MISSING')"
          echo "  - CORS: $(test -d node_modules/cors && echo 'OK' || echo 'MISSING')"
          
          # Clean up .npmrc
          rm -f .npmrc

      - name: Prepare deployment package
        run: |
          echo "📦 Creating deployment package..."
          
          # Create deployment directory
          mkdir -p deploy
          
          # Copy server files
          cp -r server/* deploy/
          
          # Copy additional resources
          cp -r shared deploy/shared 2>/dev/null || echo "No shared directory"
          cp -r knowledge-base deploy/knowledge-base 2>/dev/null || echo "No knowledge-base directory"
          
          # Create required directories
          mkdir -p deploy/app-logs/{exports,history}
          mkdir -p deploy/uploads
          
          # Clean up
          rm -f deploy/.env* deploy/tsconfig*.json
          
          # Verify deployment package
          echo "🔍 Deployment package verification:"
          echo "  - Entry point: $(test -f deploy/index.js && echo 'OK' || echo 'MISSING')"
          echo "  - Package.json: $(test -f deploy/package.json && echo 'OK' || echo 'MISSING')"
          echo "  - Node modules: $(test -d deploy/node_modules && echo 'OK' || echo 'MISSING')"
          echo "  - Express: $(test -f deploy/node_modules/express/package.json && echo 'OK' || echo 'MISSING')"

      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v3
        with:
          app-name: Emergency-Assistance
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: deploy
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          SESSION_SECRET: ${{ secrets.SESSION_SECRET }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          FRONTEND_URL: "https://witty-river-012f39e00.1.azurestaticapps.net"
          NODE_ENV: "production"
        
      - name: Health check
        run: |
          APP_URL="https://emergency-assistance-bfckhjejb3fbf9du.japanwest-01.azurewebsites.net"
          
          echo "🚀 Deployment completed"
          echo "⏰ Starting health check in 30 seconds..."
          sleep 30
          
          # Health check with retries
          for i in {1..3}; do
            echo "🔍 Health check attempt $i/3..."
            
            if curl -f "$APP_URL/api/health" --max-time 30 --silent; then
              echo "✅ Health check PASSED - Server is responding"
              exit 0
            else
              echo "⚠️ Attempt $i failed"
              if [ $i -lt 3 ]; then
                sleep 60
              fi
            fi
          done
          
          echo "❌ Health check failed after 3 attempts"
          echo "📋 Please check Azure App Service logs"
          exit 1