name: Deploy Server to Azure App Service (ZIP)

on:
  push:
    branches:
      - main
      - master
      - backup
    paths:
      - 'server/**'
      - 'shared/**'
      - 'knowledge-base/**'
      - '.github/workflows/deploy-server-azure.yml'
  workflow_dispatch:
    inputs:
      force_deploy:
        description: 'Force deployment even if no server files changed'
        required: false
        default: 'false'

permissions:
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy to Azure App Service (Node.js)
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Debug workflow trigger
        run: |
          echo "🔍 Workflow triggered by: ${{ github.event_name }}"
          echo "🔍 Branch: ${{ github.ref }}"
          echo "🔍 Commit SHA: ${{ github.sha }}"
          echo "🔍 Changed files:"
          git diff --name-only HEAD~1 HEAD || echo "No previous commit"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: server/package.json

      - name: Install dependencies and prepare package
        run: |
          echo "📦 Installing dependencies in server directory..."
          cd server
          # Clean install with all dependencies first
          npm ci --no-audit --no-fund
          echo "📦 Dependencies installed successfully"
          
          # Check if express is installed
          echo "📦 Checking express installation:"
          npm list express || echo "Express not found in npm list"
          ls -la node_modules/express/ || echo "Express directory not found"

      - name: Prepare deployment package
        run: |
          echo "📦 Preparing deployment package..."
          
          # Create deployment directory
          mkdir -p deploy
          
          # Copy all server files including node_modules
          echo "📦 Copying server files..."
          cp -r server/* deploy/
          
          # Double check node_modules copy
          echo "📦 Verifying node_modules copy:"
          if [ -d "deploy/node_modules" ]; then
            echo "✅ node_modules directory exists"
            ls -la deploy/node_modules/ | head -10
            if [ -d "deploy/node_modules/express" ]; then
              echo "✅ express module found in deployment package"
              ls -la deploy/node_modules/express/
            else
              echo "❌ express module NOT found in deployment package"
            fi
          else
            echo "❌ node_modules directory NOT copied to deploy"
            # Force copy node_modules
            echo "📦 Force copying node_modules..."
            cp -r server/node_modules deploy/
          fi
          
          # Copy shared modules
          cp -r shared deploy/
          
          # Copy knowledge base
          cp -r knowledge-base deploy/
          
          # Create necessary directories
          mkdir -p deploy/app-logs/exports
          mkdir -p deploy/app-logs/history
          mkdir -p deploy/uploads
          
          # Only remove specific dev files, keep all production dependencies
          rm -f deploy/tsconfig*.json
          rm -rf deploy/.env*
          
          echo "📦 Final deployment package contents:"
          ls -la deploy/
          echo "📦 Final node_modules check:"
          ls -la deploy/node_modules/express/ || echo "Express still missing!"
          echo "📦 Package size:"
          du -sh deploy/

      - name: Deploy to Azure App Service (using publish profile)
        uses: azure/webapps-deploy@v3
        with:
          app-name: Emergency-Assistance
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: deploy
          startup-command: 'node index.js'

      - name: Configure CORS on Azure App Service
        run: |
          echo "🌐 Configuring CORS for Azure App Service..."
          # Publish Profileを使ってCORSを設定（REST API経由）
          echo "ℹ️ CORS will be configured manually in Azure Portal"
          echo "   Add origin: https://witty-river-012f39e00.1.azurestaticapps.net"
          echo ""
          echo "💡 Current deployment uses publish profile method"
          echo "   Environment variables are managed through the deployment process"



      - name: Deploy summary
        run: |
          echo "✅ Node.js application deployed to Azure App Service"
          echo "🚀 Server deployed via ZIP deployment using Publish Profile"
          echo "🌐 Backend URL: https://emergency-assistance-bfckhjejb3fbf9du.japanwest-01.azurewebsites.net"
          echo "🌐 Frontend URL: https://witty-river-012f39e00.1.azurestaticapps.net"
          echo "✅ Environment variables set on Azure App Service"
          echo "🔧 Startup command: node index.js"
          echo ""
          echo "⚠️  Manual step required:"
          echo "   Configure CORS in Azure Portal for Emergency-Assistance App Service"
          echo "   Add allowed origin: https://witty-river-012f39e00.1.azurestaticapps.net"

