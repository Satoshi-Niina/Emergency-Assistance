name: Deploy Server to Azure App Service (ZIP)

on:
  push:
    branches:
      - main
      - master
      - backup
    paths:
      - 'server/**'
      - 'shared/**'
      - 'knowledge-base/**'
      - '.github/workflows/deploy-server-azure.yml'
  workflow_dispatch:
    inputs:
      force_deploy:
        description: 'Force deployment even if no server files changed'
        required: false
        default: 'false'
      reason:
        description: 'Reason for force deployment (Optional)'
        required: false
        default: 'Manual trigger for redeploy'

permissions:
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy to Azure App Service (Node.js)
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Debug workflow trigger
        run: |
          echo "🔍 Workflow triggered by: ${{ github.event_name }}"
          echo "🔍 Branch: ${{ github.ref }}"
          echo "🔍 Commit SHA: ${{ github.sha }}"
          echo "🔍 Changed files:"
          git diff --name-only HEAD~1 HEAD || echo "No previous commit"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Verify pre-installed dependencies
        run: |
          echo "📦 Verifying pre-installed dependencies in server directory..."
          cd server
          
          # Show current working directory and package.json
          echo "📁 Current directory: $(pwd)"
          echo "📄 Package.json exists: $(test -f package.json && echo 'YES' || echo 'NO')"
          
          # Always install dependencies (node_modules excluded by .gitignore)
          echo "📦 Installing dependencies..."
          rm -rf node_modules package-lock.json
          
          # Show package.json dependencies first
          echo "📄 Dependencies in package.json:"
          cat package.json | grep -A 20 '"dependencies"' || echo "No dependencies section found"
          
          # Install with verbose logging
          echo "📦 Running npm install..."
          npm install --no-audit --no-fund --verbose
          
          # Show installation results with detailed analysis
          echo "📦 Installation completed:"
          echo "  - Node modules count: $(ls node_modules | wc -l)"
          echo "  - Node modules size: $(du -sh node_modules/)"
          echo "  - Available modules (first 20):"
          ls node_modules/ | head -20
          echo "  - Express specifically:"
          ls -la node_modules/ | grep express || echo "No express found in directory listing"
          
          # Verify critical dependencies exist with aggressive fallback
          echo "🔍 Verifying critical dependencies..."
          CRITICAL_DEPS=("express" "cors" "dotenv" "bcryptjs" "jsonwebtoken")
          
          for dep in "${CRITICAL_DEPS[@]}"; do
            echo "🔍 Checking $dep..."
            if [ -d "node_modules/$dep" ]; then
              echo "✅ $dep: Directory exists"
              # Double-check main file exists
              if [ -f "node_modules/$dep/package.json" ]; then
                echo "✅ $dep: Package.json found"
              else
                echo "⚠️ $dep: Directory exists but package.json missing"
              fi
            else
              echo "❌ $dep: Directory missing - attempting installation..."
              
              # Try multiple installation methods
              npm install $dep --save --no-audit --no-fund
              sleep 2
              
              if [ ! -d "node_modules/$dep" ]; then
                echo "🔄 Retry: npm install $dep with force..."
                npm install $dep --save --force
                sleep 2
              fi
              
              if [ ! -d "node_modules/$dep" ]; then
                echo "🔄 Retry: npm install $dep with specific version..."
                case $dep in
                  "express") npm install express@4.21.2 --save ;;
                  "cors") npm install cors@2.8.5 --save ;;
                  "dotenv") npm install dotenv@16.6.1 --save ;;
                  "bcryptjs") npm install bcryptjs@2.4.3 --save ;;
                  "jsonwebtoken") npm install jsonwebtoken@9.0.2 --save ;;
                esac
                sleep 2
              fi
              
              # Final check
              if [ -d "node_modules/$dep" ]; then
                echo "✅ $dep: Successfully installed"
              else
                echo "❌ $dep: All installation attempts failed!"
                echo "📋 Debugging info for $dep:"
                echo "  - npm list $dep:"
                npm list $dep || echo "Not found in npm list"
                echo "  - Available modules containing '$dep':"
                ls node_modules/ | grep -i $dep || echo "No matching modules found"
                exit 1
              fi
            fi
          done
          
          # Express specific comprehensive verification
          echo "🔍 Express comprehensive verification..."
          
          if [ -f "node_modules/express/index.js" ]; then
            echo "✅ Express main file verified"
            echo "📦 Express version: $(node -p "require('./node_modules/express/package.json').version")"
          else
            echo "❌ Express main file missing!"
            echo "📋 Express diagnostic information:"
            echo "  - Express directory exists: $([ -d "node_modules/express" ] && echo 'YES' || echo 'NO')"
            if [ -d "node_modules/express" ]; then
              echo "  - Express directory contents:"
              ls -la node_modules/express/ | head -10
            fi
            
            echo "🔄 Attempting Express emergency installation..."
            
            # Remove potentially corrupted express installation
            rm -rf node_modules/express
            
            # Multiple installation attempts
            for attempt in 1 2 3; do
              echo "🔄 Express installation attempt $attempt..."
              case $attempt in
                1) npm install express@4.21.2 --save --no-audit --no-fund ;;
                2) npm install express@4.21.2 --save --force ;;
                3) npm install express --save --legacy-peer-deps ;;
              esac
              
              sleep 3
              
              if [ -f "node_modules/express/index.js" ]; then
                echo "✅ Express installed successfully on attempt $attempt"
                echo "📦 Express version: $(node -p "require('./node_modules/express/package.json').version")"
                break
              else
                echo "❌ Express installation attempt $attempt failed"
                if [ $attempt -eq 3 ]; then
                  echo "🚨 All Express installation attempts failed!"
                  echo "📋 Final diagnostic:"
                  echo "  - node_modules contents:"
                  ls -la node_modules/ | grep -i express || echo "No express-related files found"
                  echo "  - npm cache info:"
                  npm cache verify
                  exit 1
                fi
              fi
            done
          fi
          
          echo "✅ All dependencies verified and installed successfully"
          echo "📦 Final package count: $(ls node_modules | wc -l) modules"

      - name: Prepare deployment package
        run: |
          echo "📦 Preparing deployment package..."
          
          # Remove any existing deployment directory
          rm -rf deploy
          mkdir -p deploy
          
          # Copy server files (including pre-installed node_modules)
          echo "📦 Copying server source files and dependencies..."
          cp -r server/* deploy/
          
          # Copy shared modules and knowledge base
          echo "📦 Copying additional resources..."
          cp -r shared deploy/
          cp -r knowledge-base deploy/
          
          # Create necessary directories
          mkdir -p deploy/app-logs/exports
          mkdir -p deploy/app-logs/history
          mkdir -p deploy/uploads
          
          # Cleanup development files but keep all node_modules
          rm -f deploy/tsconfig*.json
          rm -rf deploy/.env*
          
          # Final verification before deployment
          echo "🔍 FINAL: Verifying deployment package..."
          
          # Check main entry point
          if [ -f "deploy/index.js" ]; then
            echo "✅ Main index.js found"
          else
            echo "❌ FATAL: Main index.js missing!"
            exit 1
          fi
          
          # Check package.json
          if [ -f "deploy/package.json" ]; then
            echo "✅ Package.json found"
            echo "📦 Start script: $(grep -A 1 '"start"' deploy/package.json)"
          else
            echo "❌ FATAL: package.json missing!"
            exit 1
          fi
          
          # Check node_modules and express
          if [ -d "deploy/node_modules" ]; then
            echo "✅ node_modules directory exists ($(du -sh deploy/node_modules/))"
            if [ -f "deploy/node_modules/express/index.js" ]; then
              echo "✅ Express module verified - main file exists"
              echo "📦 Express version: $(node -e "console.log(require('./deploy/node_modules/express/package.json').version)")"
            else
              echo "❌ FATAL: Express module incomplete!"
              echo "Available modules:"
              ls -la deploy/node_modules/ | head -10
              exit 1
            fi
          else
            echo "❌ FATAL: node_modules directory missing!"
            exit 1
          fi
          
          echo "📦 Final package verification:"
          echo "  - Total size: $(du -sh deploy/)"
          echo "  - File count: $(find deploy -type f | wc -l)"
          echo "  - JS files: $(find deploy -name '*.js' | wc -l)"
          echo "✅ Deployment package ready"

      - name: Deploy to Azure App Service (publish profile method)
        uses: azure/webapps-deploy@v3
        with:
          app-name: Emergency-Assistance
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: deploy
        env:
          # Environment variables for Azure App Service
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          SESSION_SECRET: ${{ secrets.SESSION_SECRET }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          FRONTEND_URL: "https://witty-river-012f39e00.1.azurestaticapps.net"
          NODE_ENV: "production"
        continue-on-error: false
        
      - name: Configure Azure App Service Settings
        run: |
          echo "⚙️ Verifying Azure App Service configuration..."
          echo "📦 Deployment package final verification:"
          echo "  - Deploy directory size: $(du -sh deploy/)"
          echo "  - Express module in package:"
          ls -la deploy/node_modules/express/index.js || echo "❌ Express index.js missing"
          echo "  - Main entry point:"
          ls -la deploy/index.js || echo "❌ Main index.js missing"
          echo "  - Package.json start script:"
          grep -A 5 '"scripts"' deploy/package.json || echo "❌ Scripts section missing"
          echo "✅ Configuration verification completed"
        
      - name: Verify deployment and Health Check
        run: |
          APP_URL="https://emergency-assistance-bfckhjejb3fbf9du.japanwest-01.azurewebsites.net"
          
          echo "✅ Node.js application deployed to Azure App Service"
          echo "🚀 Server deployed via ZIP deployment using Publish Profile"
          echo "🌐 Backend URL: $APP_URL"
          echo "🌐 Frontend URL: https://witty-river-012f39e00.1.azurestaticapps.net"
          echo "🎯 Startup command: node index.js"
          echo ""
          
          echo "⏰ Starting health check with retries..."
          HEALTH_CHECK_SUCCESS=false
          
          # 5回リトライ（初回30秒待機、その後60秒間隔）
          for i in {1..5}; do
            if [ $i -eq 1 ]; then
              echo "🔍 Attempt $i/5: Waiting 30 seconds for initial startup..."
              sleep 30
            else
              echo "🔍 Attempt $i/5: Waiting 60 seconds for retry..."
              sleep 60
            fi
            
            echo "🔍 Testing health check at $APP_URL/api/health..."
            if curl -f "$APP_URL/api/health" --max-time 30; then
              echo "✅ Health check PASSED on attempt $i - Server is responding correctly"
              HEALTH_CHECK_SUCCESS=true
              break
            else
              echo "❌ Health check attempt $i failed"
              echo "📋 Diagnostic info:"
              curl -I "$APP_URL/api/health" --max-time 10 || echo "No HTTP response received"
            fi
          done
          
          if [ "$HEALTH_CHECK_SUCCESS" = "true" ]; then
            echo "🎉 Deployment successful - Server is healthy!"
          else
            echo "❌ All health check attempts failed"
            echo "🔍 Please check Azure App Service logs:"
            echo "   Azure Portal → App Service → Log stream"
            echo "⚠️ Server might need more startup time or have configuration issues"
            exit 1
          fi