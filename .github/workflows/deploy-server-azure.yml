name: Deploy Server to Azure App Service (ZIP)

on:
  push:
    branches:
      - main
      - master
      - backup
    paths:
      - 'server/**'
      - 'shared/**'
      - 'knowledge-base/**'
      - '.github/workflows/deploy-server-azure.yml'
  workflow_dispatch:
    inputs:
      force_deploy:
        description: 'Force deployment even if no server files changed'
        required: false
        default: 'false'
      reason:
        description: 'Reason for force deployment (Optional)'
        required: false
        default: 'Manual trigger for redeploy'

permissions:
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy to Azure App Service (Node.js)
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Debug workflow trigger
        run: |
          echo "🔍 Workflow triggered by: ${{ github.event_name }}"
          echo "🔍 Branch: ${{ github.ref }}"
          echo "🔍 Commit SHA: ${{ github.sha }}"
          echo "🔍 Changed files:"
          git diff --name-only HEAD~1 HEAD || echo "No previous commit"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Verify pre-installed dependencies
        run: |
          echo "📦 Verifying pre-installed dependencies in server directory..."
          cd server
          
          # Show current working directory and package.json
          echo "📁 Current directory: $(pwd)"
          echo "📄 Package.json exists: $(test -f package.json && echo 'YES' || echo 'NO')"
          
          # Complete cleanup to ensure fresh start
          echo "🧹 Complete cleanup..."
          rm -rf node_modules package-lock.json npm-debug.log*
          npm cache clean --force
          
          # Verify npm registry access
          echo "🌐 Checking npm registry access..."
          npm config get registry
          npm ping || echo "⚠️ npm ping failed, but continuing..."
          
          # Install with multiple strategies
          echo "📦 Strategy 1: npm install with retries..."
          for i in {1..3}; do
            echo "📦 Attempt $i: npm install..."
            if npm install --no-optional --no-audit --no-fund; then
              echo "✅ npm install successful on attempt $i"
              break
            else
              echo "❌ npm install failed on attempt $i"
              if [ $i -eq 3 ]; then
                echo "� All npm install attempts failed, trying alternative..."
                # Try installing express specifically
                echo "📦 Installing express explicitly..."
                npm install express@^4.21.2 --save
                npm install cors@^2.8.5 --save
                npm install dotenv@^16.6.1 --save
                npm install bcryptjs@^2.4.3 --save
              else
                rm -rf node_modules package-lock.json
                sleep 5
              fi
            fi
          done
          
          # Verify critical dependencies
          echo "🔍 Verifying critical dependencies..."
          CRITICAL_DEPS=("express" "cors" "dotenv" "bcryptjs")
          ALL_DEPS_OK=true
          
          for dep in "${CRITICAL_DEPS[@]}"; do
            if [ -d "node_modules/$dep" ]; then
              echo "✅ $dep: OK"
            else
              echo "❌ $dep: MISSING - Installing individually..."
              npm install $dep --save || echo "Failed to install $dep"
              ALL_DEPS_OK=false
            fi
          done
          
          # Final verification
          echo "📦 Final dependency verification:"
          echo "  - Node.js version: $(node --version)"
          echo "  - NPM version: $(npm --version)"
          echo "  - node_modules directory:"
          ls -la node_modules/ | head -15
          echo "  - Express check:"
          if [ -f "node_modules/express/index.js" ]; then
            echo "✅ Express main file exists"
            echo "  - Express version: $(node -p "require('./node_modules/express/package.json').version")"
          else
            echo "❌ Express main file missing!"
            echo "  - Available in node_modules:"
            ls -la node_modules/ | grep express || echo "No express found"
          fi
          echo "  - Total modules: $(ls node_modules | wc -l)"
          echo "  - node_modules size: $(du -sh node_modules/)"

      - name: Prepare deployment package
        run: |
          echo "📦 Preparing deployment package..."
          
          # Remove any existing deployment directory
          rm -rf deploy
          mkdir -p deploy
          
          # Copy server files first (without node_modules)
          echo "📦 Copying server source files..."
          rsync -av --exclude='node_modules' server/ deploy/
          
          # Then copy the correctly installed node_modules
          echo "📦 Copying node_modules with verification..."
          if [ -d "server/node_modules" ]; then
            cp -r server/node_modules deploy/
            echo "✅ node_modules copied successfully"
          else
            echo "❌ ERROR: server/node_modules not found!"
            exit 1
          fi
          
          # Copy shared modules and knowledge base
          echo "📦 Copying additional resources..."
          cp -r shared deploy/
          cp -r knowledge-base deploy/
          
          # Create necessary directories
          mkdir -p deploy/app-logs/exports
          mkdir -p deploy/app-logs/history
          mkdir -p deploy/uploads
          
          # Cleanup development files but keep all node_modules
          rm -f deploy/tsconfig*.json
          rm -rf deploy/.env*
          
          # Critical verification before deployment
          echo "🔍 CRITICAL: Verifying deployment package..."
          
          # Check main entry point
          if [ -f "deploy/index.js" ]; then
            echo "✅ Main index.js found"
          else
            echo "❌ FATAL: Main index.js missing!"
            exit 1
          fi
          
          # Check package.json
          if [ -f "deploy/package.json" ]; then
            echo "✅ Package.json found"
            echo "📦 Start script: $(grep -A 1 '"start"' deploy/package.json)"
          else
            echo "❌ FATAL: package.json missing!"
            exit 1
          fi
          
          # Check node_modules and express
          if [ -d "deploy/node_modules" ]; then
            echo "✅ node_modules directory exists ($(du -sh deploy/node_modules/))"
            if [ -f "deploy/node_modules/express/index.js" ]; then
              echo "✅ Express module verified - main file exists"
              echo "📦 Express version: $(node -e "console.log(require('./deploy/node_modules/express/package.json').version)")"
            else
              echo "❌ FATAL: Express module incomplete!"
              echo "Available modules:"
              ls -la deploy/node_modules/ | head -10
              exit 1
            fi
          else
            echo "❌ FATAL: node_modules directory missing!"
            exit 1
          fi
          
          echo "📦 Final package verification:"
          echo "  - Total size: $(du -sh deploy/)"
          echo "  - File count: $(find deploy -type f | wc -l)"
          echo "  - JS files: $(find deploy -name '*.js' | wc -l)"
          echo "✅ Deployment package ready"

      - name: Deploy to Azure App Service (publish profile method)
        uses: azure/webapps-deploy@v3
        with:
          app-name: Emergency-Assistance
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: deploy
        continue-on-error: false
        
      - name: Configure Azure App Service Settings
        run: |
          echo "⚙️ Verifying Azure App Service configuration..."
          echo "📦 Deployment package final verification:"
          echo "  - Deploy directory size: $(du -sh deploy/)"
          echo "  - Express module in package:"
          ls -la deploy/node_modules/express/index.js || echo "❌ Express index.js missing"
          echo "  - Main entry point:"
          ls -la deploy/index.js || echo "❌ Main index.js missing"
          echo "  - Package.json start script:"
          grep -A 5 '"scripts"' deploy/package.json || echo "❌ Scripts section missing"
          echo "✅ Configuration verification completed"
        
      - name: Verify deployment and Health Check
        run: |
          APP_URL="https://emergency-assistance-bfckhjejb3fbf9du.japanwest-01.azurewebsites.net"
          
          echo "✅ Node.js application deployed to Azure App Service"
          echo "🚀 Server deployed via ZIP deployment using Publish Profile"
          echo "🌐 Backend URL: $APP_URL"
          echo "🌐 Frontend URL: https://witty-river-012f39e00.1.azurestaticapps.net"
          echo "� Startup command: node index.js"
          echo ""
          
          echo "⏰ Starting health check with retries..."
          HEALTH_CHECK_SUCCESS=false
          
          # 5回リトライ（初回30秒待機、その後60秒間隔）
          for i in {1..5}; do
            if [ $i -eq 1 ]; then
              echo "🔍 Attempt $i/5: Waiting 30 seconds for initial startup..."
              sleep 30
            else
              echo "🔍 Attempt $i/5: Waiting 60 seconds for retry..."
              sleep 60
            fi
            
            echo "🔍 Testing health check at $APP_URL/api/health..."
            if curl -f "$APP_URL/api/health" --max-time 30; then
              echo "✅ Health check PASSED on attempt $i - Server is responding correctly"
              HEALTH_CHECK_SUCCESS=true
              break
            else
              echo "❌ Health check attempt $i failed"
              echo "📋 Diagnostic info:"
              curl -I "$APP_URL/api/health" --max-time 10 || echo "No HTTP response received"
            fi
          done
          
          if [ "$HEALTH_CHECK_SUCCESS" = "true" ]; then
            echo "🎉 Deployment successful - Server is healthy!"
          else
            echo "❌ All health check attempts failed"
            echo "🔍 Please check Azure App Service logs:"
            echo "   Azure Portal → App Service → Log stream"
            echo "⚠️ Server might need more startup time or have configuration issues"
            exit 1
          fi

