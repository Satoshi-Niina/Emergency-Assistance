name: Deploy Server to Azure App Service (Oryx)

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch: # 手動実行も可能
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

concurrency:
  group: 'azure-appservice-deploy'
  cancel-in-progress: true

env:
  # 本番の App Service URL（例: https://xxxx.azurewebsites.net）
  APP_URL: ${{ secrets.APP_URL }}
  # ガードを一時的に無効化したい場合は "false" にする
  ZIP_GUARD: 'true'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Azureログイン
      - name: Login to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      # Zip/Artifact 系の残骸があれば明示的に失敗させる（厳密判定・コメントは無視）
      - name: Ensure no ZIP/artifact steps remain
        if: ${{ env.ZIP_GUARD != 'false' }}
        shell: bash
        run: |
          set -eo pipefail
          WF=".github/workflows/deploy-server-azure.yml"
          echo "🔎 Checking residual zip/artifact usage in ${WF}"

          # コメント行を除外して検査対象を生成
          awk '!/^[[:space:]]*#/' "${WF}" > /tmp/_wf.yml

          echo "🔎 Searching for actions upload/download-artifact..."
          if grep -niE '^[[:space:]]*uses:[[:space:]]*actions/(upload-artifact|download-artifact)@' /tmp/_wf.yml; then
            echo "❌ Found actions/upload-artifact or actions/download-artifact. Remove them."; exit 1
          fi

          echo "🔎 Searching for raw zip/unzip commands..."
          # zip/unzip コマンドのみ検出（gzip, gunzip 等にはマッチしない）
          if grep -niE '(^|[[:space:]])zip([[:space:]]|$|-)' /tmp/_wf.yml; then
            echo "❌ Found raw 'zip' command usage. Remove it."; exit 1
          fi
          if grep -niE '(^|[[:space:]])unzip([[:space:]]|$|-)' /tmp/_wf.yml; then
            echo "❌ Found raw 'unzip' command usage. Remove it."; exit 1
          fi

          echo "✅ No residual ZIP/artifact steps found."

      # AZURE_WEBAPP_NAME の確認
      - name: Validate required secrets
        shell: bash
        run: |
          echo "🔍 Checking required secrets..."
          echo ""
          
          if [ -z "${{ secrets.AZURE_WEBAPP_NAME }}" ]; then
            echo "❌ Error: AZURE_WEBAPP_NAME secret is not set"
            echo ""
            echo "📋 Setup Instructions:"
            echo "1. Go to: https://github.com/${{ github.repository }}/settings/secrets/actions"
            echo "2. Click 'New repository secret'"
            echo "3. Name: AZURE_WEBAPP_NAME"
            echo "4. Value: Your App Service name (check Azure Portal > App Service > Overview > Name)"
            echo "5. Click 'Add secret'"
            echo ""
            echo "💡 To find your App Service name:"
            echo "   - Azure Portal > App Services > Select your app > Overview tab > Name field"
            exit 1
          fi
          
          if [ -z "${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE }}" ]; then
            echo "❌ Error: AZUREAPPSERVICE_PUBLISHPROFILE secret is not set"
            echo ""
            echo "📋 Setup Instructions:"
            echo "1. Go to: https://github.com/${{ github.repository }}/settings/secrets/actions"
            echo "2. Click 'New repository secret'"
            echo "3. Name: AZUREAPPSERVICE_PUBLISHPROFILE"
            echo "4. Value: Publish Profile XML (Azure Portal > App Service > Overview > Get publish profile)"
            echo "5. Click 'Add secret'"
            exit 1
          fi
          
          echo "✅ Required secrets are set"
          echo "App Name: ${{ secrets.AZURE_WEBAPP_NAME }}"

      # Oryx Build を用いた App Service デプロイ（Zip Deploy は使用しない）
      - name: Deploy to Azure Web App (Oryx)
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE }}
          package: .

      - name: Show Kudu deployment logs link
        shell: bash
        run: |
          echo "Kudu deployments:"
          echo "https://${{ secrets.AZURE_WEBAPP_NAME }}.scm.azurewebsites.net/api/deployments"

      # 起動待ち → /live → /ready → /ping → /api/ping → /health → /api/health（成功したら即OK）
      - name: Wait for app to be ready (Health Check)
        env:
          APP_URL: ${{ env.APP_URL }}
          HEALTH_TOKEN: ${{ secrets.HEALTH_TOKEN }}
        shell: bash
        run: |
          set -eo pipefail
          if [ -z "${APP_URL}" ]; then
            echo "❌ APP_URL is not set. Define it in GitHub Secrets (APP_URL)."
            exit 1
          fi

          echo "⏰ Waiting 120 seconds for startup..."
          sleep 120

          check() {
            local url="$1"
            echo "🔍 Checking ${url}"
            if [ -n "${HEALTH_TOKEN}" ]; then
              curl -fsSL --retry 5 --retry-all-errors --retry-delay 5 \
                -H "x-health-token: ${HEALTH_TOKEN}" \
                "${url}" > /dev/null
            else
              curl -fsSL --retry 5 --retry-all-errors --retry-delay 5 \
                "${url}" > /dev/null
            fi
          }

          for p in /live /ready /ping /api/ping /health /api/health; do
            if check "${APP_URL}${p}"; then
              echo "✅ ${p} OK"
              exit 0
            fi
          done

          echo "❌ Health check failed for all endpoints."
          exit 1
