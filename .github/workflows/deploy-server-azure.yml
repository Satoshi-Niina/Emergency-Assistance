name: Deploy Server to Azure App Service (Oryx Build)

on:
  push:
    branches:
      - main
      - master
      - backup
  workflow_dispatch:

permissions:
  contents: read

env:
  APP_URL: "https://emergency-assistance-bfckhjejb3fbf9du.japanwest-01.azurewebsites.net"

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy to Azure App Service using Oryx Build

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to Azure Web App (Oryx Build)
        uses: azure/webapps-deploy@v3
        with:
          app-name: Emergency-Assistance
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: .
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          SESSION_SECRET: ${{ secrets.SESSION_SECRET }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          FRONTEND_URL: "https://witty-river-012f39e00.1.azurestaticapps.net"
          NODE_ENV: "production"
          WEBSITE_NODE_DEFAULT_VERSION: "20-lts"
          SCM_DO_BUILD_DURING_DEPLOYMENT: "true"

      - name: Wait for app to be ready
        run: |
          echo "🚀 Deployment completed"
          echo "⏰ Waiting 120 seconds for app startup..."
          sleep 120
          echo "🔍 Starting health check..."

      - name: Health check
        run: |
          # Check /ping endpoint (lightweight)
          echo "🔍 Checking /ping endpoint..."
          PING_SUCCESS=false
          
          for i in {1..5}; do
            echo "  Attempt $i/5 for /ping..."
            
            if curl -fsSL --retry 1 --retry-all-errors --max-time 10 "${{ env.APP_URL }}/ping" 2>/dev/null; then
              echo "  ✅ SUCCESS - /ping is responding"
              PING_SUCCESS=true
              break
            else
              echo "  ⚠️ Attempt $i failed for /ping"
              if [ $i -lt 5 ]; then
                echo "  ⏰ Waiting 5 seconds before retry..."
                sleep 5
              fi
            fi
          done

          if [ "$PING_SUCCESS" = false ]; then
            echo "❌ /ping endpoint failed after 5 attempts"
            echo "🔍 Trying /api/ping as fallback..."
            
            for i in {1..5}; do
              echo "  Attempt $i/5 for /api/ping..."
              
              if curl -fsSL --retry 1 --retry-all-errors --max-time 10 "${{ env.APP_URL }}/api/ping" 2>/dev/null; then
                echo "  ✅ SUCCESS - /api/ping is responding"
                PING_SUCCESS=true
                break
              else
                echo "  ⚠️ Attempt $i failed for /api/ping"
                if [ $i -lt 5 ]; then
                  sleep 5
                fi
              fi
            done
          fi

          # Check /health endpoint
          if [ "$PING_SUCCESS" = true ]; then
            echo "🔍 Checking /health endpoint..."
            HEALTH_SUCCESS=false
            
            for i in {1..5}; do
              echo "  Attempt $i/5 for /health..."
              
              if curl -fsSL --retry 1 --retry-all-errors --max-time 10 "${{ env.APP_URL }}/health" 2>/dev/null; then
                echo "  ✅ SUCCESS - /health is responding"
                HEALTH_SUCCESS=true
                break
              else
                echo "  ⚠️ Attempt $i failed for /health"
                if [ $i -lt 5 ]; then
                  sleep 5
                fi
              fi
            done

            if [ "$HEALTH_SUCCESS" = false ]; then
              echo "⚠️ /health failed, trying /api/health..."
              
              for i in {1..5}; do
                echo "  Attempt $i/5 for /api/health..."
                
                if curl -fsSL --retry 1 --retry-all-errors --max-time 10 "${{ env.APP_URL }}/api/health" 2>/dev/null; then
                  echo "  ✅ SUCCESS - /api/health is responding"
                  HEALTH_SUCCESS=true
                  break
                else
                  echo "  ⚠️ Attempt $i failed for /api/health"
                  if [ $i -lt 5 ]; then
                    sleep 5
                  fi
                fi
              done
            fi
          fi

          if [ "$PING_SUCCESS" = true ] && [ "$HEALTH_SUCCESS" = true ]; then
            echo "✅ Health check PASSED - Both ping and health endpoints are responding"
            exit 0
          else
            echo "❌ Health check failed - No endpoints responding after multiple attempts"
            echo "📋 Please check Azure App Service logs and deployment status"
            exit 1
          fi
