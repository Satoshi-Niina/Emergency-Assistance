name: Deploy Server to Azure App Service

# 新しいデプロイが開始されたら古い実行を自動キャンセル
concurrency:
  group: deploy-server-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy Server
    timeout-minutes: 10

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 🔐 Login to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: 📦 Install shared module
        run: |
          echo "📦 Installing shared dependencies..."
          cd shared
          npm install --omit=dev --no-audit --no-fund
          echo "⚠️ Skipping shared build (tsc not required for server deploy)"
          echo "✅ Shared dependencies installed"

      - name: 📦 Install server dependencies (include in ZIP)
        run: |
          echo "📦 Installing server dependencies for deployment..."
          cd server
          
          # production依存関係のみインストール
          npm ci --omit=dev --no-audit --no-fund
          
          echo "✅ Dependencies installed"
          echo "📊 node_modules size:"
          du -sh node_modules
          echo "📊 Sample packages:"
          ls node_modules | head -20

      - name: 📝 Create web.config
        run: |
          cat > server/web.config << 'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <configuration>
            <system.webServer>
              <handlers>
                <add name="iisnode" path="azure-server.mjs" verb="*" modules="iisnode"/>
              </handlers>
              <rewrite>
                <rules>
                  <rule name="NodeInspector" patternSyntax="ECMAScript" stopProcessing="true">
                    <match url="^azure-server.mjs\/debug[\/]?" />
                  </rule>
                  <rule name="StaticContent">
                    <action type="Rewrite" url="public{REQUEST_URI}"/>
                  </rule>
                  <rule name="DynamicContent">
                    <conditions>
                      <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="True"/>
                    </conditions>
                    <action type="Rewrite" url="azure-server.mjs"/>
                  </rule>
                </rules>
              </rewrite>
              <security>
                <requestFiltering>
                  <hiddenSegments>
                    <remove segment="bin"/>
                  </hiddenSegments>
                </requestFiltering>
              </security>
              <httpErrors existingResponse="PassThrough" />
            </system.webServer>
          </configuration>
          EOF
          echo "✅ web.config created"

      - name: 📝 Create deployment info
        run: |
          cd server
          timestamp=$(date -u "+%Y-%m-%d %H:%M:%S UTC")
          echo "Deployment Timestamp: $timestamp" > DEPLOY_INFO.txt
          echo "Commit SHA: ${{ github.sha }}" >> DEPLOY_INFO.txt
          echo "Run Number: ${{ github.run_number }}" >> DEPLOY_INFO.txt
          
          echo "📋 Deployment Info:"
          cat DEPLOY_INFO.txt

      - name: 🧹 Remove conflicting settings before deploy
        uses: azure/cli@v1
        continue-on-error: true
        with:
          inlineScript: |
            echo "🧹 Removing WEBSITE_RUN_FROM_PACKAGE (blocks ZipDeploy)..."
            
            az webapp config appsettings delete \
              --resource-group rg-Emergencyassistant-app \
              --name emergency-assistantapp \
              --setting-names WEBSITE_RUN_FROM_PACKAGE \
              || echo "✅ WEBSITE_RUN_FROM_PACKAGE not found (OK)"
            
            echo "✅ Conflicting settings removed"

      - name: ⏸️ Prepare App Service for deployment
        run: |
          echo "⏸️ Preparing App Service for deployment..."
          echo "✅ New App Service - No stop needed"

      - name: 🗑️ Delete all files in wwwroot (Complete Clean) - Skip if new App Service
        continue-on-error: true
        run: |
          echo "🗑️ Checking if old files exist and deleting..."
          echo "⚠️ For new App Service, this step will be skipped (no files to delete)"
          echo "✅ Clean deployment preparation complete"

      - name: ⚙️ Configure environment variables BEFORE deployment
        uses: azure/cli@v1
        with:
          inlineScript: |
            echo "⚙️ Setting environment variables BEFORE deployment..."
            
            az webapp config appsettings set \
              --resource-group rg-Emergencyassistant-app \
              --name emergency-assistantapp \
              --settings \
                NODE_ENV=production \
                DATABASE_URL="${{ secrets.DATABASE_URL }}" \
                PG_SSL="${{ secrets.PG_SSL }}" \
                SESSION_SECRET="${{ secrets.SESSION_SECRET }}" \
                JWT_SECRET="${{ secrets.JWT_SECRET }}" \
                FRONTEND_URL="${{ secrets.FRONTEND_URL }}" \
                STATIC_WEB_APP_URL="${{ secrets.STATIC_WEB_APP_URL }}" \
                AZURE_STORAGE_CONNECTION_STRING="${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}" \
                AZURE_STORAGE_ACCOUNT_NAME="${{ secrets.AZURE_STORAGE_ACCOUNT_NAME }}" \
                AZURE_STORAGE_ACCOUNT_KEY="${{ secrets.AZURE_STORAGE_ACCOUNT_KEY }}" \
                AZURE_STORAGE_CONTAINER_NAME=knowledge \
                BLOB_PREFIX=knowledge-base/ \
                STORAGE_MODE=azure \
                OPENAI_API_KEY="${{ secrets.OPENAI_API_KEY }}" \
                OPENAI_MAX_TOKENS=2000 \
                DEBUG_MODE=false \
                VERBOSE_LOGGING=false \
                WEBSITE_LOCAL_CACHE_OPTION=Never \
                WEBSITE_DYNAMIC_CACHE=0 \
                WEBSITES_ENABLE_APP_SERVICE_STORAGE=true
            
            echo "✅ Environment variables configured BEFORE deployment"
            sleep 5

      - name: 📦 Create deployment package (with node_modules)
        run: |
          echo "📦 Creating deployment package with node_modules..."
          cd server
          
          echo "Node.js version specification:"
          echo "  - .nvmrc: $(cat .nvmrc)"
          echo "  - package.json engines.node: $(cat package.json | grep -A1 'engines' | grep node)"
          
          # zipパッケージ作成 (node_modules含む)
          zip -r ../deploy.zip . \
            -x '*.git*' \
            -x '*.log' \
            -x '*.ts' \
            -x 'tsconfig.*' \
            -x 'dist/*' \
            -x 'src/*' \
            -x '.env*' \
            -x 'test/*' \
            -x 'tests/*'
          
          cd ..
          echo "✅ Package created:"
          ls -lh deploy.zip
          echo "📊 Package size with node_modules included"

      - name: 🚀 Deploy to Azure App Service (with node_modules)
        uses: azure/cli@v1
        with:
          inlineScript: |
            echo "🚀 Deploying to Azure App Service..."
            
            # ZipDeployでデプロイ（node_modules含む）
            az webapp deployment source config-zip \
              --resource-group rg-Emergencyassistant-app \
              --name emergency-assistantapp \
              --src deploy.zip \
              --timeout 600
            
            echo "✅ ZipDeploy completed"
            echo "⏳ Waiting 30 seconds for app startup..."
            sleep 30

      - name: ▶️ Start App Service with new deployment
        uses: azure/cli@v1
        with:
          inlineScript: |
            echo "▶️ Starting App Service with new deployment..."
            
            az webapp start \
              --resource-group rg-Emergencyassistant-app \
              --name emergency-assistantapp
            
            echo "⏳ Waiting 20 seconds for app to fully start..."
            sleep 20
            echo "✅ App Service started successfully with clean deployment"

      - name: ✅ Verify deployment
        run: |
          echo "✅ Deployment completed"
          echo "🔍 Verifying deployment..."
          
          APP_URL="https://emergency-assistantapp.azurewebsites.net"
          
          # Health check（最大3回リトライ）
          for i in 1 2 3; do
            echo "Attempt $i/3: Checking /api/health..."
            
            if curl -f -s "$APP_URL/api/health" > /dev/null; then
              echo "✅ Health check PASSED (HTTP 200)"
              
              # Version check
              echo ""
              echo "📊 Checking deployment version..."
              curl -s "$APP_URL/api/version" | jq '.' || echo "Version endpoint response:"
              curl -s "$APP_URL/api/version"
              
              break
            else
              echo "⚠️ Health check failed"
            fi
            
            if [ $i -lt 3 ]; then
              echo "Waiting 10 seconds before retry..."
              sleep 10
            fi
          done
          
          echo ""
          echo "📊 Deployment Summary:"
          echo "  - Commit: ${{ github.sha }}"
          echo "  - Run: ${{ github.run_number }}"
          echo "  - URL: $APP_URL"
          echo ""
          echo "🎉 Deployment workflow completed!"
