name: Deploy Server (Docker Container)

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: 'server-docker-deploy-${{ github.ref }}'
  cancel-in-progress: true

env:
  IMAGE_NAME: emergency-assistance
  NODE_VERSION: '20'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: production

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 1

    - name: Verify GitHub Repository Files
      run: |
        echo "🔍 Verifying GitHub repository files before Docker build..."
        echo "📋 Commit SHA: ${{ github.sha }}"
        echo "📋 Branch: ${{ github.ref }}"
        echo ""

        # 重要なファイルの存在確認
        CRITICAL_FILES=(
          "server/azure-server.mjs"
          "server/lib/azure-storage.ts"
          "Dockerfile"
        )

        for file in "${CRITICAL_FILES[@]}"; do
          if [ -f "$file" ]; then
            FILE_SIZE=$(wc -c < "$file" 2>/dev/null || echo "0")
            LAST_MODIFIED=$(git log -1 --format="%ai" -- "$file" 2>/dev/null || echo "unknown")
            echo "✅ $file exists (size: $FILE_SIZE bytes, last modified: $LAST_MODIFIED)"
          else
            echo "❌ ERROR: $file not found in repository!"
            exit 1
          fi
        done

        # サーバーファイルの内容確認
        echo ""
        echo "🔍 Verifying server/azure-server.mjs content..."
        if grep -q "getBlobServiceClient" server/azure-server.mjs; then
          echo "✅ getBlobServiceClient function found"
        else
          echo "❌ ERROR: getBlobServiceClient function not found!"
          exit 1
        fi

        if grep -q "Testing BLOB connection" server/azure-server.mjs; then
          echo "✅ BLOB connection test found"
        else
          echo "⚠️ WARNING: BLOB connection test not found"
        fi

        echo ""
        echo "✅ All critical files verified - Docker build will use these files"

    - name: Set Environment Variables
      id: configure-env
      run: |
        ENVIRONMENT="production"
        WEBAPP_NAME="${{ secrets.WEBAPP_NAME }}"
        RESOURCE_GROUP="${{ secrets.AZURE_RESOURCE_GROUP }}"
        APP_URL="${{ secrets.APP_URL }}"
        STATIC_WEB_APP_URL="${{ secrets.STATIC_WEB_APP_URL }}"

        export ENVIRONMENT WEBAPP_NAME RESOURCE_GROUP APP_URL STATIC_WEB_APP_URL

        {
          echo "ENVIRONMENT=$ENVIRONMENT"
          echo "WEBAPP_NAME=$WEBAPP_NAME"
          echo "RESOURCE_GROUP=$RESOURCE_GROUP"
          echo "APP_URL=$APP_URL"
          echo "STATIC_WEB_APP_URL=$STATIC_WEB_APP_URL"
        } >> "$GITHUB_ENV"

        {
          echo "environment=$ENVIRONMENT"
          echo "webapp-name=$WEBAPP_NAME"
          echo "resource-group=$RESOURCE_GROUP"
          echo "app-url=$APP_URL"
          echo "static-web-app-url=$STATIC_WEB_APP_URL"
        } >> "$GITHUB_OUTPUT"

        echo "✅ Environment: $ENVIRONMENT"
        echo "🔗 Backend: $APP_URL"
        echo "🔗 Frontend: $STATIC_WEB_APP_URL"

    - name: Validate Required Secrets
      run: |
        echo "🔍 Validating deployment secrets..."

        # Azure Resource Group
        if [ -z "$RESOURCE_GROUP" ]; then
          echo "❌ AZURE_RESOURCE_GROUP secret is missing"
          exit 1
        fi

        # Application secrets
        if [ -z "${{ secrets.DATABASE_URL }}" ]; then
          echo "❌ DATABASE_URL secret is missing"
          exit 1
        fi

        echo "✅ All required secrets validated"

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Verify Docker Build Context
      run: |
        echo "🔍 Verifying Docker build context..."
        echo "📁 Build context: . (repository root)"
        echo ""

        # Dockerfileが存在するか確認
        if [ ! -f "Dockerfile" ]; then
          echo "❌ ERROR: Dockerfile not found!"
          exit 1
        fi

        # Dockerfileの内容を確認（COPYコマンドが正しいか）
        echo "📋 Dockerfile COPY commands:"
        grep -E "^COPY|^ADD" Dockerfile || echo "No COPY/ADD commands found"
        echo ""

        # コピーされるファイルが存在するか確認
        echo "🔍 Verifying files that will be copied into Docker image:"
        if [ -d "server" ]; then
          SERVER_FILES=$(find server -type f -name "*.mjs" -o -name "*.ts" | wc -l)
          echo "✅ server directory exists ($SERVER_FILES files)"
        else
          echo "❌ ERROR: server directory not found!"
          exit 1
        fi

        if [ -f "server/azure-server.mjs" ]; then
          FILE_SIZE=$(wc -c < server/azure-server.mjs)
          echo "✅ server/azure-server.mjs exists (size: $FILE_SIZE bytes)"
        else
          echo "❌ ERROR: server/azure-server.mjs not found!"
          exit 1
        fi

        echo ""
        echo "✅ Docker build context verified - all required files are present"

    - name: Build Docker Image
      id: docker-build
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./Dockerfile
        push: false
        load: true
        no-cache: true
        tags: |
          ${{ env.IMAGE_NAME }}:${{ github.sha }}
          ${{ env.IMAGE_NAME }}:${{ steps.configure-env.outputs.environment }}-latest
        build-args: |
          NODE_ENV=${{ steps.configure-env.outputs.environment }}
          BUILDKIT_INLINE_CACHE=0

    - name: Verify Docker Image Contents
      if: success()
      run: |
        echo "🔍 Verifying Docker image contents..."
        IMAGE_TAG="${{ env.IMAGE_NAME }}:${{ github.sha }}"
        echo "📦 Image: $IMAGE_TAG"
        echo ""

        # Dockerイメージを一時的に実行してファイルを確認
        echo "🔍 Checking files in Docker image..."
        docker run --rm --entrypoint sh "$IMAGE_TAG" -c "
          echo '=== Server files in image ==='
          ls -la /app/server/ | head -20
          echo ''
          echo '=== Checking critical server files ==='
          if [ -f /app/server/azure-server.mjs ]; then
            FILE_SIZE=\$(wc -c < /app/server/azure-server.mjs)
            echo '✅ azure-server.mjs found (size: '\$FILE_SIZE' bytes)'

            # ファイルの内容を確認
            if grep -q 'getBlobServiceClient' /app/server/azure-server.mjs; then
              echo '✅ getBlobServiceClient function found'
            else
              echo '❌ ERROR: getBlobServiceClient function not found!'
              exit 1
            fi

            if grep -q 'Testing BLOB connection' /app/server/azure-server.mjs; then
              echo '✅ BLOB connection test found'
            else
              echo '⚠️ WARNING: BLOB connection test not found'
            fi
          else
            echo '❌ ERROR: azure-server.mjs not found in image!'
            exit 1
          fi

          if [ -f /app/server/lib/azure-storage.ts ]; then
            echo '✅ azure-storage.ts found'
          else
            echo '⚠️ WARNING: azure-storage.ts not found (may be compiled)'
          fi

          echo ''
          echo '=== Client dist files ==='
          if [ -d /app/client/dist ]; then
            echo '✅ client/dist directory found'
            ls -la /app/client/dist/ | head -10
          else
            echo '⚠️ WARNING: client/dist directory not found'
          fi
        " || {
          echo "⚠️ Could not verify image contents directly (this is non-critical)"
          echo "💡 Image was built successfully, verification will happen at runtime"
        }

        echo ""
        echo "✅ Docker image verification completed"
        echo "📋 Image tag: $IMAGE_TAG"
        echo "📋 Commit SHA: ${{ github.sha }}"

    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Configure App Service Environment Variables
      run: |
        echo "🔧 Configuring App Service environment variables..."

        # DATABASE_URLの検証（長さのみ、内容は表示しない）
        DB_URL="${{ secrets.DATABASE_URL }}"
        if [ -z "$DB_URL" ]; then
          echo "❌ ERROR: DATABASE_URL secret is empty!"
          exit 1
        fi
        DB_URL_LENGTH=${#DB_URL}
        echo "✅ DATABASE_URL length: $DB_URL_LENGTH characters"

        # Azure CLIのappsettings setコマンドでは、各設定を個別に設定する方が安全
        echo "🔧 Setting environment variables individually..."

        az webapp config appsettings set \
          --resource-group "$RESOURCE_GROUP" \
          --name "$WEBAPP_NAME" \
          --settings \
            NODE_ENV=$ENVIRONMENT \
            PORT=8080 \
            PG_SSL=require \
            STORAGE_MODE=hybrid \
            KNOWLEDGE_BASE_PATH=/app/knowledge-base \
            LOCAL_EXPORT_DIR=/app/knowledge-base/exports \
            FAULT_HISTORY_IMAGES_DIR=/app/knowledge-base/images/chat-exports \
            DATABASE_BACKUP=false \
            WEBSITES_PORT=8080 \
            WEBSITES_CONTAINER_START_TIME_LIMIT=600 \
            WEBSITES_ENABLE_APP_SERVICE_STORAGE=false \
          --output none

        # 機密情報は個別に設定（引用符で囲む）
        az webapp config appsettings set \
          --resource-group "$RESOURCE_GROUP" \
          --name "$WEBAPP_NAME" \
          --settings \
            DATABASE_URL="${{ secrets.DATABASE_URL }}" \
            SESSION_SECRET="${{ secrets.SESSION_SECRET }}" \
            JWT_SECRET="${{ secrets.JWT_SECRET }}" \
            OPENAI_API_KEY="${{ secrets.OPENAI_API_KEY }}" \
            AZURE_STORAGE_CONNECTION_STRING="${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}" \
            AZURE_STORAGE_CONTAINER_NAME="${{ secrets.AZURE_STORAGE_CONTAINER_NAME }}" \
            AZURE_STORAGE_ACCOUNT_NAME="${{ secrets.AZURE_STORAGE_ACCOUNT_NAME }}" \
            APPLICATIONINSIGHTS_CONNECTION_STRING="${{ secrets.APPLICATIONINSIGHTS_CONNECTION_STRING }}" \
            ApplicationInsightsAgent_EXTENSION_VERSION="${{ secrets.ApplicationInsightsAgent_EXTENSION_VERSION }}" \
            XDT_MicrosoftApplicationInsights_Mode="${{ secrets.XDT_MicrosoftApplicationInsights_Mode }}" \
          --output none

        # フロントエンドURL設定（引用符なしで設定）
        az webapp config appsettings set \
          --resource-group "$RESOURCE_GROUP" \
          --name "$WEBAPP_NAME" \
          --settings \
            FRONTEND_URL=$STATIC_WEB_APP_URL \
            STATIC_WEB_APP_URL=$STATIC_WEB_APP_URL \
            CORS_ALLOW_ORIGINS=$STATIC_WEB_APP_URL,http://localhost:5173,http://localhost:8080 \
          --output none

        # コンテナ名設定（上記の機密情報セクションで設定済みのためスキップ）
        echo "✅ AZURE_STORAGE_CONTAINER_NAME already set in credentials section"

        # AZURE_STORAGE_ACCOUNT_NAME（上記の機密情報セクションで設定済みのためスキップ）
        echo "✅ AZURE_STORAGE_ACCOUNT_NAME already set in credentials section"

        # BLOB_PREFIXが設定されている場合は追加
        if [ -n "${{ secrets.BLOB_PREFIX }}" ]; then
          az webapp config appsettings set \
            --resource-group "$RESOURCE_GROUP" \
            --name "$WEBAPP_NAME" \
            --settings \
              BLOB_PREFIX="${{ secrets.BLOB_PREFIX }}" \
            --output none
          echo "✅ BLOB_PREFIX set"
        fi

        echo "✅ Environment variables configured"

        # 設定された環境変数を確認（機密情報はマスク）
        echo ""
        echo "🔍 Verifying configured environment variables..."
        az webapp config appsettings list \
          --resource-group "$RESOURCE_GROUP" \
          --name "$WEBAPP_NAME" \
          --query "[?name=='AZURE_STORAGE_CONNECTION_STRING' || name=='AZURE_STORAGE_ACCOUNT_NAME' || name=='AZURE_STORAGE_CONTAINER_NAME' || name=='BLOB_PREFIX'].{name:name, value:value}" \
          --output table || echo "⚠️ Could not verify environment variables"

        # 環境変数の存在確認
        STORAGE_CONN_STR=$(az webapp config appsettings list \
          --resource-group "$RESOURCE_GROUP" \
          --name "$WEBAPP_NAME" \
          --query "[?name=='AZURE_STORAGE_CONNECTION_STRING'].value" \
          --output tsv 2>/dev/null || echo "")

        if [ -z "$STORAGE_CONN_STR" ]; then
          echo "❌ ERROR: AZURE_STORAGE_CONNECTION_STRING is not set in App Service!"
          exit 1
        else
          CONN_STR_LENGTH=${#STORAGE_CONN_STR}
          echo "✅ AZURE_STORAGE_CONNECTION_STRING is set (length: $CONN_STR_LENGTH)"
        fi

        STORAGE_ACCOUNT=$(az webapp config appsettings list \
          --resource-group "$RESOURCE_GROUP" \
          --name "$WEBAPP_NAME" \
          --query "[?name=='AZURE_STORAGE_ACCOUNT_NAME'].value" \
          --output tsv 2>/dev/null || echo "")

        if [ -n "$STORAGE_ACCOUNT" ]; then
          echo "✅ AZURE_STORAGE_ACCOUNT_NAME is set: $STORAGE_ACCOUNT"
        else
          echo "⚠️ AZURE_STORAGE_ACCOUNT_NAME is not set"
        fi

        STORAGE_CONTAINER=$(az webapp config appsettings list \
          --resource-group "$RESOURCE_GROUP" \
          --name "$WEBAPP_NAME" \
          --query "[?name=='AZURE_STORAGE_CONTAINER_NAME'].value" \
          --output tsv 2>/dev/null || echo "")

        if [ -n "$STORAGE_CONTAINER" ]; then
          echo "✅ AZURE_STORAGE_CONTAINER_NAME is set: $STORAGE_CONTAINER"
        else
          echo "⚠️ AZURE_STORAGE_CONTAINER_NAME is not set"
        fi

    - name: Deploy Docker Container to App Service
      run: |
        echo "🐳 Deploying Docker container to Azure App Service..."

        # Docker イメージが存在するか確認
        IMAGE_ID=$(docker images --quiet ${{ env.IMAGE_NAME }}:${{ github.sha }})

        if [ -z "$IMAGE_ID" ]; then
          echo "❌ Docker image not found: ${{ env.IMAGE_NAME }}:${{ github.sha }}"
          exit 1
        fi

        echo "✅ Docker image verified"
        echo "📦 Image ID: $IMAGE_ID"

        # デプロイパッケージの作成
        echo "📦 Creating deployment package..."
        mkdir -p /tmp/deploy
        cd /tmp/deploy
        
        # Dockerfile をコピー
        cp "$GITHUB_WORKSPACE/Dockerfile" .
        
        # server ディレクトリをコピー
        cp -r "$GITHUB_WORKSPACE/server" .
        
        # client/dist をコピー (存在する場合)
        if [ -d "$GITHUB_WORKSPACE/client/dist" ]; then
          cp -r "$GITHUB_WORKSPACE/client/dist" ./client-dist
        fi
        
        # .dockerignore をコピー (存在する場合)
        if [ -f "$GITHUB_WORKSPACE/.dockerignore" ]; then
          cp "$GITHUB_WORKSPACE/.dockerignore" .
        fi

        # package.json をコピー
        cp "$GITHUB_WORKSPACE/package.json" . 2>/dev/null || echo "⚠️ package.json not in root"

        echo "✅ Deployment package created"

        # ZIP ファイルを作成してデプロイ
        echo "📦 Creating ZIP archive..."
        zip -r -q /tmp/deploy.zip .
        echo "✅ ZIP archive created ($(du -h /tmp/deploy.zip | cut -f1))"

        # App Service にデプロイ
        echo "🚀 Deploying to App Service..."
        az webapp deployment source config-zip \
          --resource-group "$RESOURCE_GROUP" \
          --name "$WEBAPP_NAME" \
          --src /tmp/deploy.zip

        echo "✅ Deployment completed"

    - name: Restart App Service
      run: |
        echo "🔄 Restarting App Service..."

        az webapp restart \
          --name "$WEBAPP_NAME" \
          --resource-group "$RESOURCE_GROUP"

        echo "✅ App Service restart initiated"

    - name: Wait for Container Startup
      run: |
        echo "⏳ Waiting for Docker container to start (60 seconds)..."
        sleep 60

    - name: Seed Database (Production Only)
      if: github.ref == 'refs/heads/main'
      run: |
        echo "🌱 Seeding database with admin user..."

        # PostgreSQL クライアントをインストール
        sudo apt-get update -qq
        sudo apt-get install -y postgresql-client > /dev/null 2>&1

        # DATABASE_URLから接続情報を抽出して実行
        echo "Executing seed script..."
        if psql "${{ secrets.DATABASE_URL }}" -f scripts/seed-admin-user.sql 2>&1 | grep -q "シード完了"; then
          echo "✅ Database seeded successfully"
        else
          echo "⚠️  Seed script executed (check logs for details)"
        fi

    - name: Health Check
      continue-on-error: true
      run: |
        echo "🏥 Verifying deployment health..."

        MAX_RETRIES=10
        RETRY_INTERVAL=10

        for attempt in $(seq 1 $MAX_RETRIES); do
          echo "🔍 Health check attempt $attempt/$MAX_RETRIES..."

          # シンプルな /health エンドポイントをチェック
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 "$APP_URL/health" || echo "000")

          if [ "$HTTP_CODE" = "200" ]; then
            echo "✅ Health check passed (HTTP $HTTP_CODE)"

            # 詳細なヘルスチェックも実行
            echo "📊 Fetching detailed health info..."
            curl -s "$APP_URL/api/health/detailed" | head -n 20

            echo "🎉 Deployment successful and server is responding"
            exit 0
          fi

          echo "⏳ Server not ready yet (HTTP $HTTP_CODE), retrying in ${RETRY_INTERVAL}s..."
          [ $attempt -lt $MAX_RETRIES ] && sleep $RETRY_INTERVAL
        done

        echo "⚠️ Health check timeout after $MAX_RETRIES attempts"
        echo "📋 Check logs: az webapp log tail --name $WEBAPP_NAME --resource-group $RESOURCE_GROUP"
        exit 1

    - name: Deployment Summary
      if: always()
      run: |
        echo "## 🐳 Docker Deployment Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "| Item | Value |" >> $GITHUB_STEP_SUMMARY
        echo "|------|-------|" >> $GITHUB_STEP_SUMMARY
        echo "| Environment | ${ENVIRONMENT} |" >> $GITHUB_STEP_SUMMARY
        echo "| Docker Image | \`${{ env.IMAGE_NAME }}:${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
        echo "| Web App | ${WEBAPP_NAME} |" >> $GITHUB_STEP_SUMMARY
        echo "| Backend URL | ${APP_URL} |" >> $GITHUB_STEP_SUMMARY
        echo "| Frontend URL | ${STATIC_WEB_APP_URL} |" >> $GITHUB_STEP_SUMMARY
        echo "| Commit | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        if [ "${{ job.status }}" = "success" ]; then
          echo "### ✅ Deployment Successful" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Verification Commands:**" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "# Health check" >> $GITHUB_STEP_SUMMARY
          echo "curl ${APP_URL}/health" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "# Detailed health" >> $GITHUB_STEP_SUMMARY
          echo "curl ${APP_URL}/api/health/detailed | jq" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "# CORS test from frontend" >> $GITHUB_STEP_SUMMARY
          echo "curl -i -X OPTIONS ${APP_URL}/api/auth/login \\" >> $GITHUB_STEP_SUMMARY
          echo "  -H \"Origin: ${STATIC_WEB_APP_URL}\" \\" >> $GITHUB_STEP_SUMMARY
          echo '  -H "Access-Control-Request-Method: POST"' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "# View logs" >> $GITHUB_STEP_SUMMARY
          echo "az webapp log tail --name ${WEBAPP_NAME} --resource-group ${RESOURCE_GROUP}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
        else
          echo "### ❌ Deployment Failed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Troubleshooting:**" >> $GITHUB_STEP_SUMMARY
          echo "1. Check workflow logs above" >> $GITHUB_STEP_SUMMARY
          echo "2. View container logs: \`az webapp log tail --name ${WEBAPP_NAME}\`" >> $GITHUB_STEP_SUMMARY
          echo "3. Check App Service status: \`az webapp show --name ${WEBAPP_NAME} --query state\`" >> $GITHUB_STEP_SUMMARY
        fi

# Trigger: 2025-11-25 10:19:22
