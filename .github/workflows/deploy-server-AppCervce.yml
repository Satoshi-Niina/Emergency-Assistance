name: Deploy Server to Azure App Service

# 新しいデプロイが開始されたら古い実行を自動キャンセル
concurrency:
  group: deploy-server-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy Server
    timeout-minutes: 10

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 🔐 Login to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: 📦 Install shared module
        run: |
          echo "📦 Installing shared dependencies..."
          cd shared
          npm install --omit=dev --no-audit --no-fund
          echo "⚠️ Skipping shared build (tsc not required for server deploy)"
          echo "✅ Shared dependencies installed"

      - name: 📦 Install server dependencies
        run: |
          echo "📦 Installing server dependencies..."
          cd server
          
          # package-lock.jsonがあればci、なければinstall
          if [ -f package-lock.json ]; then
            npm ci --omit=dev --no-audit --no-fund
          else
            npm install --omit=dev --no-audit --no-fund
          fi
          
          echo "✅ Dependencies installed"
          echo "📊 Installed packages:"
          ls node_modules | head -20

      - name: 📝 Create web.config
        run: |
          cat > server/web.config << 'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <configuration>
            <system.webServer>
              <handlers>
                <add name="iisnode" path="azure-server.mjs" verb="*" modules="iisnode"/>
              </handlers>
              <rewrite>
                <rules>
                  <rule name="NodeInspector" patternSyntax="ECMAScript" stopProcessing="true">
                    <match url="^azure-server.mjs\/debug[\/]?" />
                  </rule>
                  <rule name="StaticContent">
                    <action type="Rewrite" url="public{REQUEST_URI}"/>
                  </rule>
                  <rule name="DynamicContent">
                    <conditions>
                      <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="True"/>
                    </conditions>
                    <action type="Rewrite" url="azure-server.mjs"/>
                  </rule>
                </rules>
              </rewrite>
              <security>
                <requestFiltering>
                  <hiddenSegments>
                    <remove segment="bin"/>
                  </hiddenSegments>
                </requestFiltering>
              </security>
              <httpErrors existingResponse="PassThrough" />
            </system.webServer>
          </configuration>
          EOF
          echo "✅ web.config created"

      - name: 📝 Create deployment info
        run: |
          cd server
          timestamp=$(date -u "+%Y-%m-%d %H:%M:%S UTC")
          echo "Deployment Timestamp: $timestamp" > DEPLOY_INFO.txt
          echo "Commit SHA: ${{ github.sha }}" >> DEPLOY_INFO.txt
          echo "Run Number: ${{ github.run_number }}" >> DEPLOY_INFO.txt
          
          echo "📋 Deployment Info:"
          cat DEPLOY_INFO.txt

      - name: ⏸️ Stop App Service for clean deployment
        uses: azure/cli@v1
        with:
          inlineScript: |
            echo "⏸️ Stopping App Service for clean deployment..."
            
            az webapp stop \
              --resource-group rg-Emergencyassistant-app \
              --name emergency-assistantapp
            
            echo "✅ App Service stopped"
            echo "⏳ Waiting 10 seconds..."
            sleep 10

      - name: 🗑️ Delete all files in wwwroot (Complete Clean)
        uses: azure/cli@v1
        continue-on-error: true
        with:
          inlineScript: |
            echo "🗑️ Deleting all files in wwwroot..."
            
            # wwwrootフォルダ内のすべてのファイルとフォルダを削除
            folders=("node_modules" "routes" "middleware" "services" "utils" "db" "config" "public")
            files=("azure-server.mjs" "package.json" "package-lock.json" "web.config" "DEPLOY_INFO.txt")
            
            # フォルダを削除
            for folder in "${folders[@]}"; do
              echo "Deleting folder: $folder"
              az rest --method delete \
                --uri "https://emergency-assistantapp.scm.azurewebsites.net/api/vfs/site/wwwroot/${folder}/?recursive=true" \
                --resource "https://management.azure.com/" \
                || echo "Folder $folder not found or already deleted"
            done
            
            # ファイルを削除
            for file in "${files[@]}"; do
              echo "Deleting file: $file"
              az rest --method delete \
                --uri "https://emergency-assistantapp.scm.azurewebsites.net/api/vfs/site/wwwroot/${file}" \
                --resource "https://management.azure.com/" \
                || echo "File $file not found or already deleted"
            done
            
            echo "✅ All old files deleted - Ready for clean deployment"

      - name: 📦 Create deployment package
        run: |
          echo "📦 Creating deployment package..."
          cd server
          
          # zipパッケージ作成 (zipコマンド使用)
          # node_modulesを含める
          # 不要なファイルを除外
          zip -r ../deploy.zip . \
            -x '*.git*' \
            -x '*.log' \
            -x '*.ts' \
            -x 'tsconfig.*' \
            -x 'dist/*' \
            -x 'src/*' \
            -x '.env*' \
            -x 'test/*' \
            -x 'tests/*'
          
          cd ..
          echo "✅ Package created:"
          ls -lh deploy.zip
          echo "📊 Package hash:"
          sha256sum deploy.zip

      - name: 🚀 Deploy to Azure App Service
        uses: azure/cli@v1
        with:
          inlineScript: |
            echo "🚀 Deploying to Azure App Service..."
            
            # ZipDeployを実行（タイムアウト10分）
            az webapp deployment source config-zip \
              --resource-group rg-Emergencyassistant-app \
              --name emergency-assistantapp \
              --src deploy.zip \
              --timeout 600
            
            echo "✅ ZipDeploy completed"

      - name: ⚙️ Configure environment variables
        uses: azure/cli@v1
        with:
          inlineScript: |
            echo "⚙️ Setting environment variables..."
            
            az webapp config appsettings set \
              --resource-group rg-Emergencyassistant-app \
              --name emergency-assistantapp \
              --settings \
                NODE_ENV=production \
                DATABASE_URL="${{ secrets.DATABASE_URL }}" \
                PG_SSL="${{ secrets.PG_SSL }}" \
                SESSION_SECRET="${{ secrets.SESSION_SECRET }}" \
                JWT_SECRET="${{ secrets.JWT_SECRET }}" \
                FRONTEND_URL="${{ secrets.FRONTEND_URL }}" \
                STATIC_WEB_APP_URL="${{ secrets.STATIC_WEB_APP_URL }}" \
                AZURE_STORAGE_CONNECTION_STRING="${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}" \
                AZURE_STORAGE_ACCOUNT_NAME="${{ secrets.AZURE_STORAGE_ACCOUNT_NAME }}" \
                AZURE_STORAGE_ACCOUNT_KEY="${{ secrets.AZURE_STORAGE_ACCOUNT_KEY }}" \
                AZURE_STORAGE_CONTAINER_NAME=knowledge \
                BLOB_PREFIX=knowledge-base/ \
                STORAGE_MODE=azure \
                OPENAI_API_KEY="${{ secrets.OPENAI_API_KEY }}" \
                OPENAI_MAX_TOKENS=2000 \
                DEBUG_MODE=false \
                VERBOSE_LOGGING=false \
                WEBSITE_RUN_FROM_PACKAGE=0 \
                SCM_DO_BUILD_DURING_DEPLOYMENT=false \
                WEBSITE_LOCAL_CACHE_OPTION=Never \
                WEBSITE_DYNAMIC_CACHE=0 \
                WEBSITES_ENABLE_APP_SERVICE_STORAGE=true \
                WEBSITE_NODE_DEFAULT_VERSION=20-lts
            
            echo "✅ Environment variables configured"

      - name: ▶️ Start App Service with new deployment
        uses: azure/cli@v1
        with:
          inlineScript: |
            echo "▶️ Starting App Service with new deployment..."
            
            az webapp start \
              --resource-group rg-Emergencyassistant-app \
              --name emergency-assistantapp
            
            echo "⏳ Waiting 20 seconds for app to fully start..."
            sleep 20
            echo "✅ App Service started successfully with clean deployment"

      - name: ✅ Verify deployment
        run: |
          echo "✅ Deployment completed"
          echo "🔍 Verifying deployment..."
          
          APP_URL="https://emergency-assistantapp-gwgscxcca5chahyb9.japanwest-01.azurewebsites.net"
          
          # Health check（最大3回リトライ）
          for i in 1 2 3; do
            echo "Attempt $i/3: Checking /api/health..."
            
            if curl -f -s "$APP_URL/api/health" > /dev/null; then
              echo "✅ Health check PASSED (HTTP 200)"
              
              # Version check
              echo ""
              echo "📊 Checking deployment version..."
              curl -s "$APP_URL/api/version" | jq '.' || echo "Version endpoint response:"
              curl -s "$APP_URL/api/version"
              
              break
            else
              echo "⚠️ Health check failed"
            fi
            
            if [ $i -lt 3 ]; then
              echo "Waiting 10 seconds before retry..."
              sleep 10
            fi
          done
          
          echo ""
          echo "📊 Deployment Summary:"
          echo "  - Commit: ${{ github.sha }}"
          echo "  - Run: ${{ github.run_number }}"
          echo "  - URL: $APP_URL"
          echo ""
          echo "🎉 Deployment workflow completed!"
