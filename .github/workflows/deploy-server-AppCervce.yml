name: Deploy Server to Azure App Service

on:
  push:
    branches:
      - main
    paths:
      - 'server/**'
      - 'shared/**'
      - '.github/workflows/deploy-server-AppCervce.yml'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 完全な履歴を取得してバージョン管理

      - name: Generate deployment metadata
        id: metadata
        run: |
          COMMIT_SHA=$(git rev-parse --short HEAD)
          COMMIT_TIME=$(git log -1 --format=%cI)
          BUILD_TIME=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          echo "commit_sha=$COMMIT_SHA" >> $GITHUB_OUTPUT
          echo "commit_time=$COMMIT_TIME" >> $GITHUB_OUTPUT
          echo "build_time=$BUILD_TIME" >> $GITHUB_OUTPUT
          echo "📋 Deployment Metadata:"
          echo "   Commit: $COMMIT_SHA"
          echo "   Time: $BUILD_TIME"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          # npm cacheを無効化して常に最新の依存関係をインストール
          # cache: 'npm'

      - name: Install shared dependencies
        run: |
          cd shared
          npm ci --omit=dev --no-cache

      - name: Install server dependencies (production only)
        run: |
          cd server
          echo "📦 Installing production dependencies..."
          npm ci --omit=dev --no-cache
          
          echo "✅ Dependencies installed:"
          ls -la node_modules | head -20
          
          echo ""
          echo "📊 node_modules size:"
          du -sh node_modules
          
      - name: Create deployment metadata file
        run: |
          cd server
          cat > deployment-info.json << EOF
          {
            "commit_sha": "${{ steps.metadata.outputs.commit_sha }}",
            "commit_time": "${{ steps.metadata.outputs.commit_time }}",
            "build_time": "${{ steps.metadata.outputs.build_time }}",
            "workflow_run": "${{ github.run_number }}",
            "deployed_by": "GitHub Actions"
          }
          EOF
          echo "📋 Created deployment-info.json:"
          cat deployment-info.json

      - name: Create web.config
        run: |
          cat > server/web.config << 'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <configuration>
            <system.webServer>
              <handlers>
                <add name="iisnode" path="azure-server.mjs" verb="*" modules="iisnode"/>
              </handlers>
              <rewrite>
                <rules>
                  <rule name="NodeInspector" patternSyntax="ECMAScript" stopProcessing="true">
                    <match url="^azure-server.mjs\/debug[\/]?" />
                  </rule>
                  <rule name="StaticContent">
                    <action type="Rewrite" url="public{REQUEST_URI}"/>
                  </rule>
                  <rule name="DynamicContent">
                    <conditions>
                      <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="True"/>
                    </conditions>
                    <action type="Rewrite" url="azure-server.mjs"/>
                  </rule>
                </rules>
              </rewrite>
              <httpErrors existingResponse="PassThrough" />
            </system.webServer>
          </configuration>
          EOF
          
          echo "✅ web.config created"

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Stop App Service (force clean deployment)
        run: |
          echo "🛑 Stopping app service for clean deployment..."
          az webapp stop --name emergency-assistantapp --resource-group rg-Emergencyassistant-app
          echo "✅ App stopped"
          sleep 15

      - name: Delete ALL old files from wwwroot
        run: |
          echo "🗑️  DELETING ALL OLD FILES FROM AZURE..."
          
          # Get deployment credentials
          CREDS=$(az webapp deployment list-publishing-credentials \
            --name emergency-assistantapp \
            --resource-group rg-Emergencyassistant-app \
            --query "{username:publishingUserName, password:publishingPassword}" \
            -o json)
          
          USERNAME=$(echo $CREDS | jq -r .username)
          PASSWORD=$(echo $CREDS | jq -r .password)
          
          echo "::add-mask::$PASSWORD"
          
          # Delete wwwroot completely via Kudu VFS API
          echo "Deleting /site/wwwroot/..."
          curl -X DELETE \
            "https://emergency-assistantapp.scm.azurewebsites.net/api/vfs/site/wwwroot/" \
            -u "$USERNAME:$PASSWORD" \
            -H "If-Match: *" \
            -w "\nHTTP_CODE:%{http_code}\n" || echo "Delete attempted"
          
          sleep 5
          
          # Verify deletion
          echo ""
          echo "Verifying wwwroot is empty..."
          RESULT=$(curl -s \
            "https://emergency-assistantapp.scm.azurewebsites.net/api/vfs/site/wwwroot/" \
            -u "$USERNAME:$PASSWORD" || echo "[]")
          
          if [ "$RESULT" = "[]" ] || [ -z "$RESULT" ]; then
            echo "✅ wwwroot successfully cleared"
          else
            echo "⚠️  wwwroot may still contain files"
          fi

      - name: Verify critical files before packaging
        run: |
          cd server
          echo "🔍 VERIFYING ALL CRITICAL FILES EXIST:"
          echo ""
          
          echo "1. Main application file:"
          ls -lh azure-server.mjs || (echo "❌ azure-server.mjs NOT FOUND" && exit 1)
          
          echo ""
          echo "2. Deployment metadata:"
          ls -lh deployment-info.json || (echo "❌ deployment-info.json NOT FOUND" && exit 1)
          cat deployment-info.json
          
          echo ""
          echo "3. Package.json:"
          ls -lh package.json || (echo "❌ package.json NOT FOUND" && exit 1)
          
          echo ""
          echo "4. node_modules (MUST EXIST):"
          if [ -d "node_modules" ]; then
            echo "✅ node_modules exists"
            ls node_modules | head -20
            du -sh node_modules
          else
            echo "❌ node_modules NOT FOUND - DEPLOYMENT WILL FAIL"
            exit 1
          fi
          
          echo ""
          echo "5. src/api structure:"
          if [ -d "src/api" ]; then
            echo "✅ src/api exists"
            find src/api -name "*.js" | head -20
          else
            echo "❌ src/api NOT FOUND"
            exit 1
          fi
          
          echo ""
          echo "6. web.config:"
          ls -lh web.config || (echo "⚠️  web.config not found" && exit 1)
          
          echo ""
          echo "📊 Total files to deploy:"
          find . -type f | wc -l

      - name: Create deployment package (COMPLETE - with node_modules)
        run: |
          cd server
          echo "📦 Creating COMPLETE deployment ZIP..."
          echo "This ZIP will contain:"
          echo "  - All source files (*.js, *.mjs)"
          echo "  - ALL node_modules (production dependencies)"
          echo "  - src/api folder"
          echo "  - deployment-info.json"
          echo "  - package.json"
          echo "  - web.config"
          echo ""
          
          # 完全なデプロイメントパッケージを作成（node_modules含む）
          # 除外するのは開発関連ファイルのみ
          zip -r ../deploy.zip . \
            -x '*.git*' \
            -x '*.log' \
            -x '*.ts' \
            -x 'tsconfig.*' \
            -x '.env*' \
            -x 'test/*' \
            -x 'tests/*' \
            -x '*.map'
          
          cd ..
          
          ZIP_SIZE=$(du -h deploy.zip | cut -f1)
          echo ""
          echo "✅ Package created: $ZIP_SIZE"
          echo ""
          
          # ZIPの内容を詳細に検証
          echo "🔍 VERIFYING ZIP CONTENTS:"
          echo ""
          
          echo "1. Critical application files:"
          unzip -l deploy.zip | grep -E "azure-server\.mjs|deployment-info\.json|package\.json|web\.config"
          
          echo ""
          echo "2. node_modules (MUST BE PRESENT):"
          NODE_MODULES_COUNT=$(unzip -l deploy.zip | grep "node_modules/" | wc -l)
          if [ $NODE_MODULES_COUNT -gt 0 ]; then
            echo "✅ node_modules IS INCLUDED ($NODE_MODULES_COUNT files)"
            unzip -l deploy.zip | grep "node_modules" | head -20
          else
            echo "❌ CRITICAL ERROR: node_modules NOT IN ZIP"
            exit 1
          fi
          
          echo ""
          echo "3. src/api files:"
          SRC_API_COUNT=$(unzip -l deploy.zip | grep "src/api" | wc -l)
          if [ $SRC_API_COUNT -gt 0 ]; then
            echo "✅ src/api IS INCLUDED ($SRC_API_COUNT files)"
            unzip -l deploy.zip | grep "src/api" | head -15
          else
            echo "❌ CRITICAL ERROR: src/api NOT IN ZIP"
            exit 1
          fi
          
          echo ""
          echo "📊 Total files in ZIP:"
          unzip -l deploy.zip | tail -1

      - name: Deploy via Azure ZipDeploy API (COMPLETE PACKAGE)
        run: |
          echo "🚀 DEPLOYING COMPLETE PACKAGE TO AZURE"
          echo "This deployment includes:"
          echo "  ✓ azure-server.mjs"
          echo "  ✓ ALL node_modules"
          echo "  ✓ src/api/*"
          echo "  ✓ deployment-info.json"
          echo "  ✓ package.json"
          echo "  ✓ web.config"
          echo ""
          
          DEPLOY_START=$(date +%s)
          
          # Get deployment credentials
          CREDS=$(az webapp deployment list-publishing-credentials \
            --name emergency-assistantapp \
            --resource-group rg-Emergencyassistant-app \
            --query "{username:publishingUserName, password:publishingPassword}" \
            -o json)
          
          USERNAME=$(echo $CREDS | jq -r .username)
          PASSWORD=$(echo $CREDS | jq -r .password)
          
          echo "::add-mask::$PASSWORD"
          
          # Deploy using ZipDeploy API
          echo "📤 Uploading ZIP (this may take 1-2 minutes)..."
          HTTP_CODE=$(curl -X POST \
            "https://emergency-assistantapp.scm.azurewebsites.net/api/zipdeploy" \
            -u "$USERNAME:$PASSWORD" \
            -H "Content-Type: application/zip" \
            --data-binary @deploy.zip \
            --max-time 600 \
            -w "%{http_code}" \
            -s \
            -o deploy_response.txt)
          
          DEPLOY_END=$(date +%s)
          DEPLOY_DURATION=$((DEPLOY_END - DEPLOY_START))
          
          echo ""
          echo "HTTP Status: $HTTP_CODE"
          echo "Duration: ${DEPLOY_DURATION}s"
          
          if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "202" ]; then
            echo "✅ Deployment upload successful"
          else
            echo "❌ Deployment failed"
            echo "Response:"
            cat deploy_response.txt
            exit 1
          fi
          
          echo ""
          echo "📄 Deployment response:"
          cat deploy_response.txt || echo "(No response body)"
          echo ""
          echo "✅ Package deployed successfully"

      - name: Start App Service
        run: |
          echo "▶️ Starting app service..."
          az webapp start --name emergency-assistantapp --resource-group rg-Emergencyassistant-app
          sleep 10
          echo "✅ App started"

      - name: Verify deployment and check commit SHA
        run: |
          echo "⏳ Waiting 45 seconds for app to fully initialize..."
          sleep 45
          
          APP_URL="https://emergency-assistantapp.azurewebsites.net"
          EXPECTED_COMMIT="${{ steps.metadata.outputs.commit_sha }}"
          
          echo ""
          echo "🔍 DEPLOYMENT VERIFICATION"
          echo "Expected commit: $EXPECTED_COMMIT"
          echo ""
          
          # Step 1: Check deployment-info.json
          echo "1. Checking deployment metadata..."
          DEPLOY_INFO=$(curl -s "$APP_URL/deployment-info.json" || echo "failed")
          
          if [ "$DEPLOY_INFO" != "failed" ]; then
            echo "✅ Deployment info accessible"
            echo "$DEPLOY_INFO" | jq .
            
            ACTUAL_COMMIT=$(echo "$DEPLOY_INFO" | jq -r .commit // jq -r .commitSha)
            
            if [ "$ACTUAL_COMMIT" = "$EXPECTED_COMMIT" ]; then
              echo "✅ COMMIT SHA MATCHES - Correct version deployed"
            else
              echo "❌ COMMIT SHA MISMATCH"
              echo "Expected: $EXPECTED_COMMIT"
              echo "Actual:   $ACTUAL_COMMIT"
              echo "⚠️  Old files may have been deployed!"
            fi
          else
            echo "⚠️  Could not fetch deployment-info.json"
          fi
          
          echo ""
          echo "2. Checking health endpoint..."
          for i in 1 2 3 4 5; do
            echo "Attempt $i/5..."
            
            HEALTH_RESPONSE=$(curl -s "$APP_URL/api/health/full" || echo "failed")
            
            if [ "$HEALTH_RESPONSE" != "failed" ]; then
              echo "✅ Server is responding"
              echo "$HEALTH_RESPONSE" | jq . || echo "$HEALTH_RESPONSE"
              
              # バージョン情報を確認
              VERSION_INFO=$(curl -s "$APP_URL/api/version" || echo "{}")
              echo "📋 Deployed version:"
              echo "$VERSION_INFO" | jq . || echo "$VERSION_INFO"
              
              # deployment-info.jsonの確認
              DEPLOY_INFO=$(curl -s "$APP_URL/deployment-info.json" || echo "{}")
              DEPLOYED_COMMIT=$(echo "$DEPLOY_INFO" | jq -r '.commit_sha' 2>/dev/null || echo "unknown")
              
              echo "📋 Deployment info:"
              echo "$DEPLOY_INFO" | jq . || echo "$DEPLOY_INFO"
              
              if [ "$DEPLOYED_COMMIT" = "$EXPECTED_COMMIT" ]; then
                echo "✅ Deployment verified: Correct version deployed"
                exit 0
              else
                echo "⚠️ Version mismatch - Expected: $EXPECTED_COMMIT, Got: $DEPLOYED_COMMIT"
              fi
            fi
            
            [ $i -lt 5 ] && sleep 20
          done
          
          echo "❌ Deployment verification failed"
          echo "Please check application logs manually"
          exit 1
