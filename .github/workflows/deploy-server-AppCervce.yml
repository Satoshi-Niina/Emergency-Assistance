name: Deploy Server (Azure App Service)

# 新しいデプロイが開始されたら古い実行を自動キャンセル
concurrency:
  group: deploy-server-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - main
    # paths設定を削除 - mainへのプッシュで常にデプロイ
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy Server to Azure App Service
    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: false
          fetch-depth: 1
          lfs: false

      - name: Verify package-lock.json exists
        run: |
          echo "📋 Checking for package-lock.json files..."
          ls -la package-lock.json || echo "❌ Root package-lock.json not found"
          ls -la client/package-lock.json || echo "❌ Client package-lock.json not found"
          ls -la server/package-lock.json || echo "⚠️ Server package-lock.json not found (expected)"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Login to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Install and build shared module (first)
        run: |
          echo "📦 Installing shared dependencies..."
          cd shared
          npm install --no-audit --no-fund
          echo "🔨 Building shared module..."
          npm run build
          echo "✅ Shared module built"

      - name: Install server dependencies
        run: |
          echo "📦 Installing server dependencies..."
          cd server
          
          # 古いnode_modulesを削除してクリーンインストール
          echo "🧹 Cleaning old node_modules..."
          rm -rf node_modules
          
          # 本番用の依存関係をインストール（package-lock.jsonがない場合はnpm install）
          if [ -f "package-lock.json" ]; then
            echo "Using npm ci with package-lock.json"
            npm ci --omit=dev --no-audit --no-fund
          else
            echo "⚠️ package-lock.json not found, using npm install"
            npm install --omit=dev --no-audit --no-fund
          fi
          
          echo "✅ Server dependencies installed"
          echo "📋 Verifying .mjs files exist:"
          ls -lh *.mjs | head -5

      - name: Create web.config for Azure App Service
        run: |
          cat > server/web.config << 'EOF'
          <?xml version="1.0" encoding="utf-8"?>
          <configuration>
            <system.webServer>
              <handlers>
                <add name="iisnode" path="azure-server.mjs" verb="*" modules="iisnode"/>
              </handlers>
              <rewrite>
                <rules>
                  <rule name="NodeInspector" patternSyntax="ECMAScript" stopProcessing="true">
                    <match url="^azure-server.mjs\/debug[\/]?" />
                  </rule>
                  <rule name="StaticContent">
                    <action type="Rewrite" url="public{REQUEST_URI}"/>
                  </rule>
                  <rule name="DynamicContent">
                    <conditions>
                      <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="True"/>
                    </conditions>
                    <action type="Rewrite" url="azure-server.mjs"/>
                  </rule>
                </rules>
              </rewrite>
              <security>
                <requestFiltering>
                  <hiddenSegments>
                    <remove segment="bin"/>
                  </hiddenSegments>
                </requestFiltering>
              </security>
              <httpErrors existingResponse="PassThrough" />
            </system.webServer>
          </configuration>
          EOF
          echo "✅ web.config created"
      
      - name: Create deployment package (all source files)
        timeout-minutes: 5
        run: |
          echo "📦 Creating fresh deployment package..."
          cd server
          
          # 一意のタイムスタンプを生成
          TIMESTAMP=$(date -u +%Y%m%d_%H%M%S)
          COMMIT_SHORT=$(echo "${{ github.sha }}" | cut -c1-8)
          
          echo "Deployment Timestamp: $TIMESTAMP" > DEPLOY_INFO.txt
          echo "Git Commit: $COMMIT_SHORT" >> DEPLOY_INFO.txt
          echo "Workflow Run: ${{ github.run_number }}" >> DEPLOY_INFO.txt
          echo "Random ID: $RANDOM" >> DEPLOY_INFO.txt
          
          # 全ソースファイルをzipに含める（node_modules除外、Azure側でnpm install）
          echo "Including all source files for fresh deployment..."
          zip -r ../server-deploy.zip \
            package.json \
            package-lock.json \
            azure-server.mjs \
            app.js \
            server.js \
            fallback-server.js \
            web.config \
            DEPLOY_INFO.txt \
            routes/ \
            middleware/ \
            services/ \
            utils/ \
            config/ \
            db/ \
            lib/ \
            types/ \
            -x "*.git*" "*.log" ".npm/*" "*.ts" "tsconfig.*" "*.test.*" "*.spec.*" "dist/*" "node_modules/*" "uploads/*"
          
          cd ..
          echo "✅ Deployment zip created: server-deploy.zip"
          ls -lh server-deploy.zip
          
          # zipの内容を確認
          echo "📋 Package contents:"
          unzip -l server-deploy.zip
          
          # azure-server.mjsが含まれているか確認
          echo "🔍 Verifying azure-server.mjs in package:"
          unzip -l server-deploy.zip | grep "azure-server.mjs" || echo "❌ ERROR: azure-server.mjs not found in package!"

      - name: Set environment variables BEFORE deployment
        uses: azure/cli@v1
        with:
          inlineScript: |
            echo "⚙️ Setting app settings BEFORE deployment..."
            
            az webapp config appsettings set \
              --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
              --name ${{ secrets.AZURE_WEBAPP_NAME }} \
              --settings \
                WEBSITE_RUN_FROM_PACKAGE=0 \
                SCM_DO_BUILD_DURING_DEPLOYMENT=true \
                WEBSITE_LOCAL_CACHE_OPTION=Never \
                WEBSITE_DYNAMIC_CACHE=0 \
                WEBSITES_ENABLE_APP_SERVICE_STORAGE=true \
                WEBSITE_NODE_DEFAULT_VERSION=20-lts
            
            echo "✅ Critical deployment settings configured"

      - name: Stop App Service (確実に古いプロセスを停止)
        uses: azure/cli@v1
        with:
          inlineScript: |
            echo "🛑 Stopping App Service to clear old processes..."
            az webapp stop \
              --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
              --name ${{ secrets.AZURE_WEBAPP_NAME }}
            sleep 5
            echo "✅ App Service stopped"

      - name: Clear old deployment files via Kudu
        continue-on-error: true
        run: |
          echo "🗑️ Clearing old deployment files via Kudu API..."
          
          # Kudu認証情報取得
          PUBLISH_PROFILE=$(az webapp deployment list-publishing-profiles \
            --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
            --name ${{ secrets.AZURE_WEBAPP_NAME }} \
            --xml)
          
          KUDU_USER=$(echo "$PUBLISH_PROFILE" | grep -oP '(?<=userName=")[^"]+' | head -1)
          KUDU_PASS=$(echo "$PUBLISH_PROFILE" | grep -oP '(?<=userPWD=")[^"]+' | head -1)
          
          KUDU_URL="https://${{ secrets.AZURE_WEBAPP_NAME }}.scm.azurewebsites.net"
          
          echo "Attempting to delete old files: azure-server.mjs, package.json"
          
          # 古いファイルを削除（エラーは無視）
          curl -X DELETE "$KUDU_URL/api/vfs/site/wwwroot/azure-server.mjs" \
            -u "$KUDU_USER:$KUDU_PASS" || echo "azure-server.mjs not found or already deleted"
          
          curl -X DELETE "$KUDU_URL/api/vfs/site/wwwroot/package.json" \
            -u "$KUDU_USER:$KUDU_PASS" || echo "package.json not found or already deleted"
          
          echo "✅ Old files cleared (if they existed)"

      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v2
        with:
          app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
          package: server-deploy.zip

      - name: Wait for deployment extraction
        run: |
          echo "⏳ デプロイファイル展開を待機（20秒）..."
          sleep 20

      - name: Set remaining environment variables
        uses: azure/cli@v1
        with:
          inlineScript: |
            echo "⚙️ Setting application environment variables..."
            
            az webapp config appsettings set \
              --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
              --name ${{ secrets.AZURE_WEBAPP_NAME }} \
              --settings \
                NODE_ENV=production \
                DATABASE_URL="${{ secrets.DATABASE_URL }}" \
                PG_SSL="${{ secrets.PG_SSL }}" \
                SESSION_SECRET="${{ secrets.SESSION_SECRET }}" \
                JWT_SECRET="${{ secrets.JWT_SECRET }}" \
                FRONTEND_URL="${{ secrets.FRONTEND_URL }}" \
                STATIC_WEB_APP_URL="${{ secrets.STATIC_WEB_APP_URL }}" \
                AZURE_STORAGE_CONNECTION_STRING="${{ secrets.AZURE_STORAGE_CONNECTION_STRING }}" \
                AZURE_STORAGE_ACCOUNT_NAME="${{ secrets.AZURE_STORAGE_ACCOUNT_NAME }}" \
                AZURE_STORAGE_ACCOUNT_KEY="${{ secrets.AZURE_STORAGE_ACCOUNT_KEY }}" \
                AZURE_STORAGE_CONTAINER_NAME=knowledge \
                BLOB_PREFIX=knowledge-base/ \
                STORAGE_MODE=azure \
                OPENAI_API_KEY="${{ secrets.OPENAI_API_KEY }}" \
                OPENAI_MAX_TOKENS=2000 \
                DEBUG_MODE=false \
                VERBOSE_LOGGING=false
            
            echo "✅ Application settings configured"

      - name: Seed admin user to database
        uses: azure/cli@v1
        with:
          inlineScript: |
            echo "🌱 Seeding admin user to production database..."
            
            # PostgreSQLクライアントをインストール
            apt-get update -qq && apt-get install -y postgresql-client > /dev/null 2>&1
            
            # SQLスクリプトを実行
            psql "${{ secrets.DATABASE_URL }}" -c "
              INSERT INTO users (username, password, role, display_name, department, created_at, updated_at)
              VALUES (
                'niina',
                '\$2a\$10\$w9kGZVYX8yqK5YJ3p7H.4eN8Z9M0xQ1K2L3M4N5O6P7Q8R9S0T1U2V',
                'admin',
                '管理者',
                '情報システム部',
                NOW(),
                NOW()
              )
              ON CONFLICT (username) DO UPDATE SET
                password = EXCLUDED.password,
                updated_at = NOW();
            " || echo "⚠️ User seed may have failed, but continuing..."
            
            echo "✅ Admin user seeded"

      - name: Start App Service with new files
        uses: azure/cli@v1
        with:
          inlineScript: |
            echo "▶️ Starting App Service with new deployment..."
            az webapp start \
              --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
              --name ${{ secrets.AZURE_WEBAPP_NAME }}
            
            echo "⏳ Waiting 40 seconds for app to fully start..."
            sleep 40
            echo "✅ App Service started with new files"

      - name: Verify deployment
        run: |
          echo "✅ Deployment completed"
          echo "🔍 Checking application health..."
          
          # ヘルスチェック
          HEALTH_URL="https://${{ secrets.AZURE_WEBAPP_NAME }}.azurewebsites.net/health"
          echo "🏥 Checking health endpoint: $HEALTH_URL"
          
          RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" "$HEALTH_URL" || echo "000")
          
          if [ "$RESPONSE" = "200" ]; then
            echo "✅ Health check passed (HTTP $RESPONSE)"
          else
            echo "⚠️ Health check returned HTTP $RESPONSE"
            echo "   This may be normal if the app is still starting up"
          fi

# Trigger: 2025-11-30 15:30 - Deploy with environment variable management
