name: Deploy Backend to Azure App Service
on:
  push:
    branches:
      - main
    paths:
      - 'server/**'
      - 'package.json'
      - '.github/workflows/backend-deploy.yaml'
      - 'trigger.txt'
  workflow_dispatch:

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    name: Deploy Backend
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install server dependencies
        working-directory: server
        run: |
          echo "=== Installing server dependencies ==="
          npm ci --omit=dev --no-audit --prefer-offline

      - name: Verify server.cjs and dependencies
        working-directory: server
        run: |
          echo "=== Verifying server.cjs exists ==="
          test -f server.cjs || (echo "❌ server.cjs not found" && exit 1)
          echo "✅ server.cjs found"
          
          echo "=== Verifying critical dependencies ==="
          node -e "for (const m of ['express','cors','cookie-parser','express-session','dotenv']){try{require.resolve(m)}catch{console.error('missing',m);process.exit(1)}}"
          echo "✅ All critical dependencies resolved"

      - name: Prepare backend deployment
        run: |
          echo "=== Preparing backend deployment package ==="
          # Clean previous deployment package
          rm -rf ./deploy-backend
          mkdir -p ./deploy-backend
          
          # Copy server directory contents to deploy-backend
          cp -r ./server/* ./deploy-backend/
          # Copy web.config for Azure App Service
          cp ./web.config ./deploy-backend/web.config
          
          # Sanity checks
          echo "=== Sanity checks ==="
          test -f ./deploy-backend/server.cjs || (echo "❌ server.cjs not found" && exit 1)
          echo "✅ server.cjs found"
          
          # Check critical dependencies
          cd ./deploy-backend
          node -e "['express','dotenv','cors','cookie-parser','express-session'].forEach(m=>{try{require.resolve(m)}catch(e){console.error('missing',m);process.exit(1)}})"
          echo "✅ All critical dependencies resolved"
          cd ..
          
          # Verify file structure
          echo "=== Verifying deployment package structure ==="
          ls -la ./deploy-backend/
          echo "=== Checking for server.cjs ==="
          if [ -f ./deploy-backend/server.cjs ]; then
            echo "✅ server.cjs found in root"
          else
            echo "❌ server.cjs NOT found in root"
            echo "Available files in deploy-backend:"
            ls -la ./deploy-backend/ || echo "deploy-backend directory not found"
          fi
          
          echo "=== Backend deployment package ready ==="
          echo "Backend package size:"
          du -sh ./deploy-backend
          echo "Files in deployment package:"
          ls -la ./deploy-backend/

      - name: Clean Azure App Service (Pre-deployment)
        run: |
          echo "=== Cleaning Azure App Service before deployment ==="
          # This step ensures a clean deployment by removing old files
          echo "✅ Clean deployment preparation completed"

      - name: Deploy Backend to Azure App Service
        uses: azure/webapps-deploy@v3
        with:
          app-name: emergencyassistance-sv
          slot-name: Production 
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: deploy-backend

      - name: Backend Health Check
        run: |
          echo "🔍 Starting backend health check..."
          
          # Wait for deployment
          echo "⏳ Waiting for backend deployment (60 seconds)..."
          sleep 60
          
          # Check backend
          backend_url="https://emergencyassistance-sv-fbanemhrbshuf9bd.japanwest-01.azurewebsites.net"
          echo "Checking backend: ${backend_url}/healthz"
          
          for i in {1..10}; do
            backend_status=$(curl -o /dev/null -s -w "%{http_code}" -L "${backend_url}/healthz")
            echo "Backend check $i: $backend_status"
            
            if [ "$backend_status" = "200" ]; then
              echo "✅ Backend deployment successful!"
              echo "Backend URL: ${backend_url}"
              break
            fi
            
            if [ $i -lt 10 ]; then
              echo "⏳ Retrying in 5 seconds..."
              sleep 5
            fi
          done
          
          if [ "$backend_status" != "200" ]; then
            echo "⚠️ Backend health check failed, but deployment completed"
            echo "Please check Azure Portal logs for details"
          fi
