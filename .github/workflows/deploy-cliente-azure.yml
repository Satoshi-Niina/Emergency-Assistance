name: Deploy Client (Azure Static Web Apps)

on:
  push:
    branches: [main, develop]
    paths:
      - 'client/**'
      - '.github/workflows/deploy-cliente-azure.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: false
        default: 'production'
        type: choice
        options:
          - production
          - staging

concurrency:
  group: 'client-deploy-${{ github.ref }}'
  cancel-in-progress: true

env:
  NODE_VERSION: '20'

jobs:
  deploy-client:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set Environment Variables
        run: |
          # 環境判定: プッシュイベントの場合はブランチ名、workflow_dispatchの場合は入力値を使用
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            ENV_INPUT="${{ github.event.inputs.environment }}"
            if [[ "$ENV_INPUT" == "production" ]] || [[ -z "$ENV_INPUT" ]]; then
              ENV_TYPE="production"
            else
              ENV_TYPE="staging"
            fi
          elif [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            ENV_TYPE="production"
          else
            ENV_TYPE="staging"
          fi

          if [[ "$ENV_TYPE" == "production" ]]; then
            echo "ENVIRONMENT=production" >> $GITHUB_ENV
            echo "STATIC_WEB_APP_TOKEN=${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}" >> $GITHUB_ENV
            echo "BACKEND_URL=https://emergency-assistance-bfckhjejb3fbf9du.japanwest-01.azurewebsites.net" >> $GITHUB_ENV
            echo "STATIC_WEB_APP_URL=https://witty-river-012f39e00.1.azurestaticapps.net" >> $GITHUB_ENV
            echo "✅ Environment: production"
            echo "🔗 Backend: https://emergency-assistance-bfckhjejb3fbf9du.japanwest-01.azurewebsites.net"
          else
            echo "ENVIRONMENT=staging" >> $GITHUB_ENV
            STAGING_TOKEN="${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_STAGING }}"
            if [ -n "$STAGING_TOKEN" ] && [ "$STAGING_TOKEN" != "" ]; then
              echo "STATIC_WEB_APP_TOKEN=$STAGING_TOKEN" >> $GITHUB_ENV
            else
              echo "STATIC_WEB_APP_TOKEN=${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}" >> $GITHUB_ENV
            fi
            echo "BACKEND_URL=https://emergency-assistance-staging.japanwest-01.azurewebsites.net" >> $GITHUB_ENV
            echo "STATIC_WEB_APP_URL=https://staging.witty-river-012f39e00.1.azurestaticapps.net" >> $GITHUB_ENV
            echo "✅ Environment: staging"
            echo "🔗 Backend: https://emergency-assistance-staging.japanwest-01.azurewebsites.net"
          fi

      - name: Validate Secrets
        run: |
          if [ -z "$STATIC_WEB_APP_TOKEN" ]; then
            echo "❌ ERROR: Azure Static Web Apps token not found"
            echo "Required secrets for $ENVIRONMENT:"
            if [ "$ENVIRONMENT" = "production" ]; then
              echo "- AZURE_STATIC_WEB_APPS_API_TOKEN"
            else
              echo "- AZURE_STATIC_WEB_APPS_API_TOKEN_STAGING or AZURE_STATIC_WEB_APPS_API_TOKEN"
            fi
            exit 1
          fi
          echo "✅ Secrets validated"

      - name: Setup Node.js with Enhanced Caching
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: client/package-lock.json

      - name: Install Dependencies
        working-directory: ./client
        run: |
          echo "📦 Installing client dependencies..."
          npm ci --prefer-offline --no-audit --no-fund

      - name: Build Client
        working-directory: ./client
        run: |
          echo "🔨 Building client for $ENVIRONMENT environment..."
          echo "   Backend URL: $BACKEND_URL"

          # Clear build cache to ensure fresh build
          echo "🧹 Clearing build cache completely..."
          rm -rf dist node_modules/.vite .vite .cache .parcel-cache || true
          npm cache clean --force || true
          
          # Verify source file has correct changes
          echo "🔍 Verifying source file changes..."
          if grep -q "hasSecurityAccess" src/pages/settings.tsx; then
            echo "❌ ERROR: Source file still contains hasSecurityAccess!"
            exit 1
          fi
          if ! grep -q "セキュリティー監視" src/pages/settings.tsx; then
            echo "❌ ERROR: Security card not found in source file!"
            exit 1
          fi
          if ! grep -q "保守管理" src/pages/settings.tsx; then
            echo "❌ ERROR: Maintenance card not found in source file!"
            exit 1
          fi
          echo "✅ Source file verification passed"

          # Set environment variables for Vite build
          export NODE_ENV=production
          export VITE_BACKEND_SERVICE_URL="$BACKEND_URL"
          export VITE_API_BASE_URL="$BACKEND_URL/api"
          export VITE_ENVIRONMENT="$ENVIRONMENT"
          # Force new build ID to bypass cache
          export VITE_BUILD_ID="$(date +%s)-${{ github.sha }}"

          echo "   API Base URL: $VITE_API_BASE_URL"
          echo "   Build ID: $VITE_BUILD_ID"

          # Build with no cache
          echo "🔨 Running build command..."
          npm run build 2>&1 | tee build.log || {
            echo "❌ Build failed!"
            cat build.log
            exit 1
          }
          
          # Verify build output immediately after build
          echo "🔍 Immediate verification of build output..."
          if [ ! -f "dist/index.html" ]; then
            echo "❌ ERROR: dist/index.html not found immediately after build!"
            ls -la dist/ || echo "dist directory not found"
            exit 1
          fi
          
          # Check if cards are in the built file
          MAIN_FILE=$(ls dist/main.*.mjs 2>/dev/null | head -1)
          if [ -z "$MAIN_FILE" ]; then
            echo "❌ ERROR: main.*.mjs file not found!"
            ls -la dist/
            exit 1
          fi
          
          echo "📄 Checking built file: $MAIN_FILE"
          if grep -q "セキュリティー監視" "$MAIN_FILE"; then
            echo "✅ Security card found in $MAIN_FILE"
          else
            echo "❌ ERROR: Security card NOT found in $MAIN_FILE!"
            echo "File size: $(wc -c < "$MAIN_FILE") bytes"
            exit 1
          fi
          
          if grep -q "保守管理" "$MAIN_FILE"; then
            echo "✅ Maintenance card found in $MAIN_FILE"
          else
            echo "❌ ERROR: Maintenance card NOT found in $MAIN_FILE!"
            exit 1
          fi
          
          if grep -q "hasSecurityAccess" "$MAIN_FILE"; then
            echo "❌ ERROR: hasSecurityAccess still found in $MAIN_FILE!"
            grep -n "hasSecurityAccess" "$MAIN_FILE" | head -5
            exit 1
          else
            echo "✅ hasSecurityAccess removed from $MAIN_FILE"
          fi

          # ビルド結果検証
          if [ ! -f "dist/index.html" ]; then
            echo "❌ Client build failed - index.html not found"
            ls -la dist/ || echo "dist directory not found"
            exit 1
          fi

          # ハッシュ付きファイル名に対応
          required_files=("index.html" "runtime-config.js")
          for file in "${required_files[@]}"; do
            if [ ! -f "dist/$file" ]; then
              echo "❌ ERROR: Required file missing after build: dist/$file"
              exit 1
            fi
          done

          # ハッシュ付きファイルの存在確認
          if ! ls dist/main.*.mjs 1> /dev/null 2>&1; then
            echo "❌ ERROR: Required file missing after build: dist/main.*.mjs"
            ls -la dist/ | grep -E "\.(mjs|js)$" || echo "No .mjs or .js files found"
            exit 1
          fi

          if ! ls dist/style.*.css 1> /dev/null 2>&1; then
            echo "❌ ERROR: Required file missing after build: dist/style.*.css"
            ls -la dist/ | grep -E "\.css$" || echo "No .css files found"
            exit 1
          fi

          file_count=$(find dist -type f | wc -l)
          echo "✅ Client build completed successfully: $file_count files"

          echo "📊 Build Statistics:"
          echo "   Files: $file_count"
          echo "   Size: $(du -sh dist/ | cut -f1)"
          echo "   Contents:"
          ls -la dist/

          # Azure Static Web Apps file limit check and optimization
          if [ $file_count -gt 1000 ]; then
            echo "❌ ERROR: File count ($file_count) exceeds Azure Static Web Apps limit (1000 files)"
            echo "🔧 Optimizing build output..."

            # Remove source maps if they exist
            find dist -name "*.map" -delete || true

            # Remove unnecessary files
            find dist -name "*.txt" -not -name "robots.txt" -delete || true
            find dist -name "*.md" -delete || true

            # Recount files
            file_count=$(find dist -type f | wc -l)
            echo "📦 Optimized file count: $file_count"

            if [ $file_count -gt 1000 ]; then
              echo "❌ Still too many files after optimization. Manual intervention required."
              exit 1
            fi
          elif [ $file_count -gt 100 ]; then
            echo "⚠️ WARNING: File count ($file_count) is high but acceptable"
            echo "🔧 Consider further build optimization for better performance"
          else
            echo "✅ File count ($file_count) is optimal for Azure Static Web Apps"
          fi

      - name: Update Configuration Files
        run: |
          echo "🔧 Updating runtime configuration..."

          # client/dist/runtime-config.js の更新
          if [ -f "client/dist/runtime-config.js" ]; then
            echo "🔄 Updating runtime-config.js..."
            echo "window.runtimeConfig = { backendUrl: '$BACKEND_URL', environment: '$ENVIRONMENT' };" > client/dist/runtime-config.js
            echo "✅ Runtime config updated"
          else
            echo "⚠️ runtime-config.js not found, creating..."
            echo "window.runtimeConfig = { backendUrl: '$BACKEND_URL', environment: '$ENVIRONMENT' };" > client/dist/runtime-config.js
          fi

          # index.htmlにビルドバージョンを追加してキャッシュを強制的に無効化
          if [ -f "client/dist/index.html" ]; then
            echo "🔄 Adding build version to index.html..."
            SHA_SHORT="${{ github.sha }}"
            BUILD_VERSION="v$(date +%s)-${SHA_SHORT:0:7}"
            sed -i "s|<html lang=\"ja\">|<html lang=\"ja\" data-build=\"$BUILD_VERSION\">|g" client/dist/index.html
            # スクリプトタグにもバージョンを追加
            sed -i "s|<script type=\"module\"|<script type=\"module\" data-version=\"$BUILD_VERSION\"|g" client/dist/index.html
            echo "✅ Build version added: $BUILD_VERSION"
          fi

      - name: Inject Backend URL
        working-directory: ./client
        run: |
          echo "🔧 Injecting backend URL into runtime configuration..."

          if [ -f "dist/index.html" ]; then
            # window.BACKEND_SERVICE_URLを置換
            if grep -q "window.BACKEND_SERVICE_URL" dist/index.html; then
              # 既存のwindow.BACKEND_SERVICE_URLの値を置換
              sed -i "s|window.BACKEND_SERVICE_URL = '[^']*'|window.BACKEND_SERVICE_URL = '$BACKEND_URL'|g" dist/index.html
              echo "✅ Backend URL injected: $BACKEND_URL"
            # プレースホルダーがある場合は置換
            elif grep -q "{{BACKEND_URL}}" dist/index.html; then
              sed -i "s|{{BACKEND_URL}}|$BACKEND_URL|g" dist/index.html
              echo "✅ Backend URL injected via placeholder: $BACKEND_URL"
            else
              echo "ℹ️ No window.BACKEND_SERVICE_URL or placeholder found for backend URL injection"
            fi
          else
            echo "⚠️ dist/index.html not found, skipping injection"
          fi

      - name: Verify Build Output
        working-directory: ./client
        run: |
          echo "📦 Verifying build output..."

          # Copy staticwebapp.config.json to dist
          cp staticwebapp.config.json dist/

          echo "📊 Final deployment package:"
          ls -lah dist/
          file_count=$(find dist -type f | wc -l)
          size=$(du -sh dist/ | cut -f1)
          echo "   Files: $file_count"
          echo "   Size: $size"

          # Remove source maps to reduce file count
          find dist -name "*.map" -delete || true

          file_count_after=$(find dist -type f | wc -l)
          echo "   Files after optimization: $file_count_after"

          if [ $file_count_after -gt 1000 ]; then
            echo "⚠️ WARNING: File count ($file_count_after) exceeds Azure Static Web Apps limit"
          else
            echo "✅ File count ($file_count_after) is optimal"
          fi

      - name: Verify Build Output Before Deploy
        working-directory: ./client
        run: |
          echo "📦 Verifying build output before deployment..."

          # distフォルダの存在確認
          if [ ! -d "dist" ]; then
            echo "❌ ERROR: dist directory not found!"
            exit 1
          fi

          # 必須ファイルの確認
          required_files=("index.html" "main.*.mjs" "style.*.css")
          for pattern in "${required_files[@]}"; do
            if ! ls dist/$pattern 1> /dev/null 2>&1; then
              echo "❌ ERROR: Required file not found: dist/$pattern"
              ls -la dist/ || echo "dist directory listing failed"
              exit 1
            fi
          done

          # settings.tsxの変更が反映されているか確認（ビルドされたファイルにhasSecurityAccessが含まれていないか）
          echo "🔍 Checking if settings page changes are in build..."
          if find dist -name "*.mjs" -exec grep -l "hasSecurityAccess" {} \; 2>/dev/null | grep -q .; then
            echo "❌ ERROR: hasSecurityAccess found in built files - old code is still present!"
            echo "This means the build is using cached or old code."
            echo "Files containing hasSecurityAccess:"
            find dist -name "*.mjs" -exec grep -l "hasSecurityAccess" {} \; 2>/dev/null
            exit 1
          else
            echo "✅ hasSecurityAccess not found in built files - changes are included"
          fi
          
          # カードが確実に含まれているか確認
          echo "🔍 Verifying cards are in build..."
          if find dist -name "*.mjs" -exec grep -l "セキュリティー監視" {} \; 2>/dev/null | grep -q .; then
            echo "✅ Security card found in build"
          else
            echo "❌ ERROR: Security card not found in build!"
            exit 1
          fi
          
          if find dist -name "*.mjs" -exec grep -l "保守管理" {} \; 2>/dev/null | grep -q .; then
            echo "✅ Maintenance card found in build"
          else
            echo "❌ ERROR: Maintenance card not found in build!"
            exit 1
          fi
          
          # 権限チェックが削除されているか確認
          echo "🔍 Verifying permission checks are removed..."
          if find dist -name "*.mjs" -exec grep -l "hasSecurityAccess.*&&" {} \; 2>/dev/null | grep -q .; then
            echo "❌ ERROR: Permission check (hasSecurityAccess &&) still found in build!"
            exit 1
          else
            echo "✅ Permission checks removed from build"
          fi

          # ファイル一覧を表示
          echo "📊 Build output files:"
          ls -lah dist/ | head -20

          echo "✅ Build output verification completed"

      - name: Clear Azure Static Web Apps Cache
        run: |
          echo "🗑️ Clearing deployment cache..."
          # Force fresh deployment by removing any cache markers
          rm -rf ~/.azure-static-web-apps-cli || true
          # Clear npm cache
          npm cache clean --force || true

      - name: Verify Files Before Deploy
        run: |
          echo "📋 Verifying files before deployment..."
          echo "Current directory: $(pwd)"
          echo "Working directory: ${{ github.workspace }}"

          # 絶対パスで確認
          if [ ! -d "${{ github.workspace }}/client/dist" ]; then
            echo "❌ ERROR: client/dist directory does not exist at ${{ github.workspace }}/client/dist"
            echo "Listing client directory:"
            ls -lah client/ || echo "client directory not found"
            exit 1
          fi

          echo "Checking client/dist directory:"
          ls -lah client/dist/ || echo "❌ client/dist directory not found!"

          if [ ! -f "client/dist/index.html" ]; then
            echo "❌ ERROR: client/dist/index.html not found!"
            echo "Files in client/dist:"
            ls -lah client/dist/ || echo "Cannot list files"
            exit 1
          fi

          echo "✅ Required files verified"
          echo "File count in client/dist: $(find client/dist -type f | wc -l)"
          echo "Directory size: $(du -sh client/dist | cut -f1)"

      - name: Deploy to Azure Static Web Apps
        id: deploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          app_location: client/dist
          output_location: ''
          skip_app_build: true
          skip_api_build: true
          production_branch: main
          deployment_environment: production
        continue-on-error: false
        env:
          DEBUG: '*'

      - name: Verify Deployment
        if: success()
        run: |
          echo "✅ Deployment completed successfully"
          echo "🔗 Deployed to: $STATIC_WEB_APP_URL"
          echo "⏰ Timestamp: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "🔢 Build ID: $VITE_BUILD_ID"
          echo ""
          echo "⚠️ Note: Azure Static Web Apps may take 2-3 minutes to propagate changes globally"
          echo "💡 If changes are not visible immediately, please:"
          echo "   1. Wait 2-3 minutes for CDN cache to clear"
          echo "   2. Hard refresh your browser (Ctrl+Shift+R or Cmd+Shift+R)"
          echo "   3. Clear browser cache if needed"

      - name: Deployment Error Details
        if: failure()
        run: |
          echo "❌ Deployment failed!"
          echo "📋 Debug information:"
          echo "  Working directory: $(pwd)"
          echo "  Client dist exists: $([ -d 'client/dist' ] && echo 'yes' || echo 'no')"
          echo "  Index.html exists: $([ -f 'client/dist/index.html' ] && echo 'yes' || echo 'no')"
          echo "  Token set: $([ -n '${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}' ] && echo 'yes' || echo 'no')"
          echo ""
          echo "📁 Contents of client/dist:"
          ls -lah client/dist/ || echo "Directory not found"
          echo ""
          echo "🔍 Check the step above for detailed error messages"

      - name: Trigger CDN Cache Purge
        continue-on-error: true
        run: |
          echo "🗑️ Requesting CDN cache purge..."
          # Wait a bit for deployment to propagate
          sleep 10

          # Try to purge Azure CDN cache by making requests with cache-busting headers
          # Use timestamp query parameter to force cache miss
          TIMESTAMP=$(date +%s)

          curl -X GET "$STATIC_WEB_APP_URL/?v=$TIMESTAMP" \
            -H "Cache-Control: no-cache, no-store, must-revalidate" \
            -H "Pragma: no-cache" \
            -H "Expires: 0" \
            -s -o /dev/null -w "Status: %{http_code}\n" || true

          curl -X GET "$STATIC_WEB_APP_URL/index.html?v=$TIMESTAMP" \
            -H "Cache-Control: no-cache, no-store, must-revalidate" \
            -H "Pragma: no-cache" \
            -H "Expires: 0" \
            -s -o /dev/null -w "Status: %{http_code}\n" || true

          echo "✅ Cache purge request sent with timestamp: $TIMESTAMP"

      - name: Health Check
        env:
          MAX_RETRIES: 5
          RETRY_INTERVAL: 20
        run: |
          echo "🏥 Running health check for $ENVIRONMENT environment..."

          # ヘルスチェック関数
          health_check() {
            local url="$1"
            echo "Checking: $url"

            response=$(curl -s -w "%{http_code}" "$url" -o /dev/null || echo "000")
            if [[ "$response" =~ ^[23] ]]; then
              echo "✅ Health check passed ($response)"
              return 0
            else
              echo "❌ Health check failed ($response)"
              return 1
            fi
          }

          # ヘルスチェック実行
          success=false
          for ((i=1; i<=$MAX_RETRIES; i++)); do
            echo "🔄 Attempt $i/$MAX_RETRIES..."

            if health_check "$STATIC_WEB_APP_URL"; then
              success=true
              break
            fi

            if [ $i -lt $MAX_RETRIES ]; then
              echo "⏳ Waiting $RETRY_INTERVAL seconds before retry..."
              sleep $RETRY_INTERVAL
            fi
          done

          if [ "$success" = false ]; then
            echo "❌ Health check failed after $MAX_RETRIES attempts"
            exit 1
          fi

      - name: Integration Test with Backend
        if: github.ref == 'refs/heads/main'
        run: |
          echo "🧪 Running integration tests with backend..."

          # バックエンドとの接続テスト
          backend_health() {
            curl -s --max-time 10 "$BACKEND_URL/api/health" || return 1
          }

          if backend_health; then
            echo "✅ Backend integration test passed"
          else
            echo "⚠️ Backend integration test failed - continuing deployment"
          fi

      - name: Deployment Summary
        if: always()
        run: |
          echo ""
          echo "📋 CLIENT DEPLOYMENT SUMMARY"
          echo "============================="
          echo "🕐 Completed: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "🌍 Environment: $ENVIRONMENT"
          echo "🔗 Static Web App: $STATIC_WEB_APP_URL"
          echo "🔗 Backend API: $BACKEND_URL"
          echo "🎯 Status: ${{ job.status }}"
          echo ""
          echo "📍 Next Steps:"
          echo "1. Visit: $STATIC_WEB_APP_URL"
          echo "2. Test key features"
          echo "3. Monitor logs if issues occur"
