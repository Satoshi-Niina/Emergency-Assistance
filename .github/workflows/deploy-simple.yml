name: Simple Deploy to Azure App Service

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Server Dependencies
        working-directory: ./server
        run: |
          npm ci --production=false
          echo "✅ Server dependencies installed"

      - name: Build Client
        working-directory: ./client
        run: |
          npm ci
          npm run build
          echo "✅ Client built successfully"

      - name: Prepare Deployment Package
        shell: bash
        run: |
          echo "📦 Creating deployment package..."
          mkdir -p deploy-package

          # Copy server files to deployment package
          echo "Copying server files..."
          tar --exclude='node_modules' -cf - -C server . | tar -xf - -C deploy-package

          # Copy client build
          echo "Copying client build..."
          mkdir -p deploy-package/client
          if [ -d "client/dist" ]; then
            tar -cf - -C client/dist . | tar -xf - -C deploy-package/client/dist
          else
            echo "❌ Client dist not found"
            exit 1
          fi

          echo "📋 Deployment structure verification:"
          ls -la deploy-package/
          echo "📁 Essential files check:"
          [ -f "deploy-package/package.json" ] && echo "✅ package.json" || echo "❌ package.json missing"
          [ -f "deploy-package/azure-server.js" ] && echo "✅ azure-server.js" || echo "❌ azure-server.js missing"
          [ -d "deploy-package/client/dist" ] && echo "✅ client/dist" || echo "❌ client/dist missing"

      - name: Deploy to Azure
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'Emergency-Assistance'
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: ./deploy-package

      - name: Test Deployment
        run: |
          echo "🔍 Testing deployment..."
          sleep 30
          curl -f https://emergency-assistance-bfckhjejb3fbf9du.japanwest-01.azurewebsites.net/ping || echo "❌ Health check failed"
