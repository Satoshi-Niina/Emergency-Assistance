name: Build and Deploy Server (Docker)

on:
  push:
    branches:
      - main
    paths:
      - 'server/**'
      - 'shared/**'
      - 'knowledge-base/**'
      - 'Dockerfile'
      - 'server/Dockerfile'
      - '.github/workflows/deploy-server.yml'
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        # デプロイ先のコンテナレジストリに応じて変更してください
        # 例: Docker Hub
        # uses: docker/login-action@v3
        # with:
        #   username: ${{ secrets.DOCKER_USERNAME }}
        #   password: ${{ secrets.DOCKER_PASSWORD }}
        
        # 例: GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/server
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./server
          file: ./server/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=ghcr.io/${{ github.repository }}/server:buildcache
          cache-to: type=registry,ref=ghcr.io/${{ github.repository }}/server:buildcache,mode=max
          platforms: linux/amd64,linux/arm64

      # デプロイステップはデプロイ先に応じてカスタマイズしてください
      # 例: AWS ECS
      # - name: Configure AWS credentials
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     aws-region: ${{ secrets.AWS_REGION }}
      # 
      # - name: Deploy to ECS
      #   run: |
      #     aws ecs update-service \
      #       --cluster ${{ secrets.ECS_CLUSTER_NAME }} \
      #       --service ${{ secrets.ECS_SERVICE_NAME }} \
      #       --force-new-deployment \
      #       --region ${{ secrets.AWS_REGION }}

      # 例: Azure Container Instances
      # - name: Azure Login
      #   uses: azure/login@v1
      #   with:
      #     creds: ${{ secrets.AZURE_CREDENTIALS }}
      # 
      # - name: Deploy to Azure Container Instances
      #   run: |
      #     az container create \
      #       --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
      #       --name emergency-assistance-server \
      #       --image ghcr.io/${{ github.repository }}/server:${{ github.sha }} \
      #       --dns-name-label emergency-assistance-${{ github.run_number }} \
      #       --ports 8080 \
      #       --environment-variables \
      #         NODE_ENV=production \
      #         PORT=8080 \
      #         DATABASE_URL="${{ secrets.DATABASE_URL }}" \
      #         JWT_SECRET="${{ secrets.JWT_SECRET }}" \
      #         SESSION_SECRET="${{ secrets.SESSION_SECRET }}" \
      #         OPENAI_API_KEY="${{ secrets.OPENAI_API_KEY }}"

      # 例: Google Cloud Run
      # - name: Authenticate to Google Cloud
      #   uses: google-github-actions/auth@v2
      #   with:
      #     credentials_json: ${{ secrets.GCP_SA_KEY }}
      # 
      # - name: Set up Cloud SDK
      #   uses: google-github-actions/setup-gcloud@v2
      # 
      # - name: Deploy to Cloud Run
      #   run: |
      #     gcloud run deploy emergency-assistance-server \
      #       --image ghcr.io/${{ github.repository }}/server:${{ github.sha }} \
      #       --region ${{ secrets.GCP_REGION }} \
      #       --platform managed \
      #       --allow-unauthenticated \
      #       --set-env-vars \
      #         NODE_ENV=production,PORT=8080 \
      #       --set-secrets \
      #         DATABASE_URL=DATABASE_URL:latest,JWT_SECRET=JWT_SECRET:latest,SESSION_SECRET=SESSION_SECRET:latest,OPENAI_API_KEY=OPENAI_API_KEY:latest

      - name: Deploy summary
        run: |
          echo "✅ Docker image built and pushed successfully"
          echo "📦 Image: ghcr.io/${{ github.repository }}/server"
          echo "🔧 Configure deployment steps for your hosting provider above"

