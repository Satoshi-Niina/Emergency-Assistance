name: Deploy backend to Azure Web App

on:
  push:
    branches:
      - main  # デプロイ対象のブランチ
  workflow_dispatch:

env:
  AZURE_WEBAPP_NAME: emergency-backend              # AzureのApp Service名と一致
  AZURE_WEBAPP_PACKAGE_PATH: dist                     # ビルド出力フォルダ（React/Viteなら dist）
  NODE_VERSION: '20'                                  # 使用する Node.js のバージョン

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ✅ Checkout repo
        uses: actions/checkout@v4

      - name: 🔧 Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📦 Install dependencies
        run: npm install

      - name: 🏗️ Build the project
        run: npm run build

      - name: 🚀 Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}  # Secretsで設定済みの発行プロファイルを参照
          package: ${{ env.AZURE_WEBAPP_PACKAGE_PATH }}

