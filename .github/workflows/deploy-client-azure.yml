name: Deploy Client to Azure Static Web Apps

on:
  push:
    branches:
      - main
    paths:
      - 'client/**'
      - '.github/workflows/deploy-client-azure.yml'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    name: Build and Deploy Static Web App
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Clean previous build
        working-directory: ./client
        run: |
          rm -rf dist node_modules/.vite
          echo "✅ Cleaned previous build artifacts"

      - name: Install dependencies
        working-directory: ./client
        run: npm ci

      - name: Build client
        working-directory: ./client
        env:
          NODE_ENV: production
          VITE_API_BASE_URL: ${{ secrets.VITE_API_BASE_URL || '/api' }}
        run: npm run build

      - name: Verify build output
        working-directory: ./client
        run: |
          echo "📁 Build output directory contents:"
          ls -la dist/ || echo "❌ dist directory not found"
          echo ""
          echo "📁 Checking for runtime-config.js:"
          if [ -f "dist/runtime-config.js" ]; then
            echo "✅ runtime-config.js found in dist"
            ls -la dist/runtime-config.js
            echo "📄 First 10 lines:"
            head -10 dist/runtime-config.js
          else
            echo "⚠️ runtime-config.js NOT found in dist"
            echo "📁 Checking public directory:"
            ls -la public/ || echo "❌ public directory not found"
            if [ -f "public/runtime-config.js" ]; then
              echo "✅ Found in public, copying to dist..."
              cp public/runtime-config.js dist/
              ls -la dist/runtime-config.js
            else
              echo "❌ ERROR: runtime-config.js not found in public/"
              exit 1
            fi
          fi

      - name: Copy staticwebapp.config.json to dist
        working-directory: ./client
        run: |
          if [ -f "staticwebapp.config.json" ]; then
            cp staticwebapp.config.json dist/
            echo "✅ Copied staticwebapp.config.json to dist"
            ls -la dist/staticwebapp.config.json
            echo "📄 Verifying staticwebapp.config.json content:"
            cat dist/staticwebapp.config.json | head -20
          else
            echo "❌ ERROR: staticwebapp.config.json not found"
            exit 1
          fi
          # Ensure staticwebapp.config.json is valid JSON
          python3 -m json.tool dist/staticwebapp.config.json > /dev/null && echo "✅ JSON is valid" || echo "❌ ERROR: Invalid JSON"

      - name: Final verification before deploy
        working-directory: ./client
        run: |
          echo "📦 Final dist directory contents:"
          ls -la dist/
          echo ""
          echo "🔍 Checking critical files:"
          [ -f "dist/index.html" ] && echo "✅ index.html" || echo "❌ index.html missing"
          [ -f "dist/runtime-config.js" ] && echo "✅ runtime-config.js" || echo "❌ runtime-config.js missing"
          [ -f "dist/staticwebapp.config.json" ] && echo "✅ staticwebapp.config.json" || echo "❌ staticwebapp.config.json missing"
          [ -d "dist/assets" ] && echo "✅ assets directory" || echo "⚠️ assets directory missing"
          echo ""
          echo "📄 Checking index.html content:"
          if [ -f "dist/index.html" ]; then
            echo "--- index.html content (last 20 lines) ---"
            tail -20 dist/index.html
            echo ""
            echo "🔍 Checking for script tags:"
            grep -i "script" dist/index.html || echo "⚠️ No script tags found"
            echo ""
            echo "🔍 Checking for /src/main.tsx reference:"
            if grep -q "/src/main.tsx" dist/index.html; then
              echo "❌ ERROR: /src/main.tsx still referenced in built index.html!"
              echo "This should have been replaced by Vite with /assets/main-*.js"
              echo "Vite build may have failed or index.html was not processed correctly"
            else
              echo "✅ No /src/main.tsx reference found (correctly replaced)"
            fi
          fi
          echo ""
          echo "📁 Assets directory contents:"
          ls -la dist/assets/ 2>/dev/null | head -10 || echo "⚠️ assets directory not found or empty"

      - name: Deploy to Azure Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "/client"
          output_location: "dist"
          skip_app_build: true

      - name: Deploy summary
        run: |
          echo "✅ Client deployed to Azure Static Web Apps"
          echo "🌐 Frontend URL: https://witty-river-012f39e00.1.azurestaticapps.net"

