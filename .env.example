# Example environment variables for Emergency-Assistance
# Copy this file to `.env` and fill values for local development (do NOT commit real secrets).

# General
NODE_ENV=development
PORT=8080

# Server / Database
DATABASE_URL=
PG_SSL=disable

# Auth / Sessions
JWT_SECRET=
SESSION_SECRET=
BYPASS_DB_FOR_LOGIN=false

# OpenAI / External APIs
OPENAI_API_KEY=

# Azure Blob Storage 設定 (例)
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_CONTAINER_NAME=knowledge
BLOB_PREFIX=Azure-knowledge/knowledge-base/

# Frontend (Vite) - these will be exposed to client via import.meta.env
# VITE_API_BASE_URL:
# - Development: leave empty (set to an empty string) so the client uses relative paths like `/api/...`.
#                With Vite dev server the client will proxy `/api` to the backend. Do NOT set an absolute host in dev.
# - Production: set to the absolute API base URL, e.g. `https://api.example.com`.
#                When set, client requests will be prefixed: `${VITE_API_BASE_URL}/api/...`.
# Recommended defaults for local development:
VITE_API_BASE_URL=
# If you need to bypass auth locally for tests set this to true (client only flag)
VITE_AUTH_BYPASS=false

# CORS / Origins
FRONTEND_URL=http://localhost:5173
CORS_ALLOW_ORIGINS=http://localhost:5173,http://localhost:5174
